# View functions

`/view/*` (ch16) controls model view, selection, and capture — the same
`"Argument"`-wrapped, one-`TypedDict`-parameter convention as
`midas_nx.ope`.

::: midas_nx.view
    options:
      members:
        - select_by_identity
        - capture_view
