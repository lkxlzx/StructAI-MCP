# /db/EIGV
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-eigv
endpoint: /db/EIGV
name: Eigenvalue Analysis Control
surface:
  className: EigenvalueAnalysisControl
  exportName: eigenvalueAnalysisControl
  modulePath: [db, analysisControl]
  payloadTypeName: EigenvalueAnalysisControlPayload
  nestedTypes:
    - {path: vRITZ, name: EigenvalueRitzLoadCaseItem, namespace: DbAnalysisControlTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 5. /db/EIGV — Eigenvalue Analysis Control
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-16
    - product: gen
      ref: db-write-sweep-2026-08-16

operations:
  - method: DELETE
    variant: per_id
    path: /db/EIGV/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: TYPE
    description: >-
      Type of Analysis (Subspace Iteration: "EIGEN" / Lanczos: "LANCZOS" / Ritz
      Vectors: "RITZ")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

variants:
  - when:
      - path: TYPE
        equals: EIGEN
    source:
      table: "Parameters — Eigen Vectors (Subspace Iteration, `TYPE = \"EIGEN\"`)"
      line: 427
    fields:
      - key: iFREQ
        description: >-
          Number of Frequencies
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: iITER
        description: >-
          Number of Iterations
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: iDIM
        description: >-
          Subspace Dimension
        type: integer
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: TOL
        description: >-
          Convergence Tolerance
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
  - when:
      - path: TYPE
        equals: LANCZOS
    source:
      table: "Parameters — Eigen Vectors (Lanczos, `TYPE = \"LANCZOS\"`)"
      line: 436
    fields:
      - key: iFREQ
        description: >-
          Number of Frequencies
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: bMINMAX
        description: >-
          Frequency Range of Interest
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: FRMIN
        description: >-
          Search From [cps] (when bMINMAX is true, FRMIN < FRMAX)
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: FRMAX
        description: >-
          Search To [cps] (when bMINMAX is true)
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: bSTRUM
        description: >-
          Sturm Sequence Check
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
  - when:
      - path: TYPE
        equals: RITZ
    source:
      table: "Parameters — Ritz Vectors (`TYPE = \"RITZ\"`)"
      line: 446
    fields:
      - key: bINCNL
        description: >-
          Include GL-link Force Vectors
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: iGNUM
        description: >-
          Number of Generations for Each GL-link Force
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: vRITZ
        description: >-
          Load Cases
        type: array
        items:
          type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: KIND
            description: >-
              Load Case Type (Ground Acc.: "GROUND" / General: "CASE")
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vRITZ' by numbering it '(1)', not by
            # naming a path
          - key: CASE
            description: >-
              Load Case Name (KIND="CASE"일 때)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vRITZ' by numbering it '(2)', not by
            # naming a path
          - key: GROUND
            description: >-
              Ground Acc. 방향 (KIND="GROUND"일 때, "ACCX"/"ACCY"/"ACCZ")
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vRITZ' by numbering it '(3)', not by
            # naming a path
          - key: iNOG
            description: >-
              Number of Generations
            type: integer
            requirement: optional
            documentedDefault: ""
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vRITZ' by numbering it '(4)', not by
            # naming a path

sdkRules:
  - id: db-eigv-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-eigv-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 421
  table: Parameters — 공통
