# /DESIGN/RC/KDS-41-20-2022/SDGN
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-sdgn
endpoint: /DESIGN/RC/KDS-41-20-2022/SDGN
name: Seismic Design Type
surface:
  className: SeismicDesignType
  exportName: seismicDesignType
  modulePath: [design, rcKds, setup]
  payloadTypeName: SeismicDesignTypePayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "9. `DESIGN/RC/KDS-41-20-2022/SDGN` — Seismic Design Type (내진 설계 타입)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼 (부재 ID 문자열 키)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: NTYPE
    description: >-
      내진 설계 타입 (Seismic / Non-Seismic / Non-Seismic-Force-Resisting)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [Seismic, Non-Seismic, Non-Seismic-Force-Resisting]
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 1996
  table: 파라미터
