# /DESIGN/STEEL/KDS-41-30-2022/TABLE
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-table
endpoint: /DESIGN/STEEL/KDS-41-30-2022/TABLE
name: Steel Member Design Forces

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "27. `DESIGN/STEEL/KDS-41-30-2022/TABLE` — Steel Member Design Forces (강재 부재 설계 부재력)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-11
    - product: gen
      ref: db-read-sweep-2026-08-11

operations:
  - method: POST
    surface:
      exportName: getSteelMemberDesignForcesTable
      modulePath: [design, steelKds]
      argumentTypeName: SteelMemberDesignForcesArgument
      documentation: |-
        docs/manual/25_Design_Steel_KDS41302022.md #27 — TABLE — Steel Member
        Design Forces. Response: ``{table_name_or_"empty": {"FORCE": ...,
        "DIST": ..., "HEAD": [...], "DATA": [[...], ...]}}``.

        Shares the ``TABLE_TYPE``-crash pattern seen across this whole
        Design-Forces family — presumed at risk on
        Gen until independently confirmed on real data. Re-tested 2026-08-11
        on Gen NX (v2.1, build 08/11/2026), blank ``/doc/NEW`` document,
        ``{"TABLE_TYPE": "STEELMEMBERDESIGNFORCES"}``: clean empty response,
        no crash, session stayed healthy. A single clean pass is not
        independent proof the risk is gone — treat as reduced-but-not-cleared
        until a real-model retest.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: TABLE_NAME
    description: >-
      응답 표 제목
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: TABLE_TYPE
    description: >-
      표 타입 ("STEELMEMBERDESIGNFORCES" 고정)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [STEELMEMBERDESIGNFORCES]
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      결과 파일 저장 경로
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: UNIT
    description: >-
      단위 설정 (FORCE/DIST/HEAT/TEMP)
    type: object
    requirement: optional
    documentedDefault: System
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: STYLES
    description: >-
      숫자 포맷 (FORMAT: Default/Fixed/Scientific/General, PLACE 0~15)
    type: object
    requirement: optional
    documentedDefault: System
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: COMPONENTS
    description: >-
      결과 표 성분 (Index,Memb,Part,LComName,Type,Fx,Fy,Fz,Mx,My,Mz)
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: NODE_ELEMS
    description: >-
      노드/요소 선택 (KEYS/TO/STRUCTURE_GROUP_NAME 중 하나)
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: PARTS
    description: >-
      요소 파트 (PartI,Part1/4,Part2/4,Part3/4,PartJ)
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: ["All"]
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 6088
  table: 파라미터
