# /db/PNLD
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-pnld
endpoint: /db/PNLD
name: Define Plane Load Type
surface:
  className: PlaneLoadType
  exportName: planeLoadType
  modulePath: [db, staticLoads]
  payloadTypeName: PlaneLoadTypePayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 06_DB_Static_Loads.md
    section: 11. /db/PNLD — Define Plane Load Type
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-16
    - product: gen
      ref: db-write-sweep-2026-08-16

operations:
  - method: DELETE
    variant: per_id
    path: /db/PNLD/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      Load Type Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DESC
    description: >-
      Description
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: LTYPE
    description: >-
      Load Type ("POINT" / "LINE" / "AREA")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: COPY_X
    description: >-
      Copy in X-Direction
    type: array
    items:
      type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: COPY_Y
    description: >-
      Copy in Y-Direction
    type: array
    items:
      type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SEQ
    description: >-
      Sequence Number (Unique)
    type: integer
    requirement: optional
    documentedDefault: null
    documentedDefaultNote: Auto
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's confirmed PlaneLoadType case completed
      create/read/update/read/delete on Gen and Civil without this field. The
      public SDK sends no endpoint-specific normalization for this record.
    provenance: manual

variants:
  - when:
      - path: LTYPE
        equals: POINT
    source:
      table: "LTYPE = \"POINT\""
      line: 979
    fields:
      - key: POINTLOAD
        description: >-
          Point Loads
        type: array
        items:
          type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: X
            description: >-
              Point X
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'POINTLOAD' by numbering it '(1)', not
            # by naming a path
          - key: Y
            description: >-
              Point Y
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'POINTLOAD' by numbering it '(2)', not
            # by naming a path
          - key: F
            description: >-
              Force
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'POINTLOAD' by numbering it '(3)', not
            # by naming a path
  - when:
      - path: LTYPE
        equals: LINE
    source:
      table: "LTYPE = \"LINE\""
      line: 984
    fields:
      - key: LINELOAD
        description: >-
          Line Loads
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: bUNIFORM
            description: >-
              Uniform (true) / Trapezoidal (false)
            type: boolean
            requirement: optional
            documentedDefault: true
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'LINELOAD' by numbering it '(1)', not
            # by naming a path
          - key: X
            description: >-
              Coordinates of X1, X2
            type: array
            items:
              type: number
            minItems: 2
            maxItems: 2
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'LINELOAD' by numbering it '(2)', not
            # by naming a path
          - key: Y
            description: >-
              Coordinates of Y1, Y2
            type: array
            items:
              type: number
            minItems: 2
            maxItems: 2
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'LINELOAD' by numbering it '(3)', not
            # by naming a path
          - key: F
            description: >-
              Force (Uniform: [F1] / Trap: [F1, F2])
            type: array
            items:
              type: number
            minItems: 2
            maxItems: 2
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'LINELOAD' by numbering it '(4)', not
            # by naming a path
  - when:
      - path: LTYPE
        equals: AREA
    source:
      table: "LTYPE = \"AREA\""
      line: 990
    fields:
      - key: AREALOAD
        description: >-
          Area Loads
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: bUNIFORM
            description: >-
              Uniform (true) / Trapezoidal (false)
            type: boolean
            requirement: optional
            documentedDefault: false
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'AREALOAD' by numbering it '(1)', not
            # by naming a path
          - key: b3PNT
            description: >-
              3 Points (true) / 4 Points (false)
            type: boolean
            requirement: optional
            documentedDefault: false
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'AREALOAD' by numbering it '(2)', not
            # by naming a path
          - key: X
            description: >-
              Coordinates X1~X4
            type: array
            items:
              type: number
            minItems: 4
            maxItems: 4
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'AREALOAD' by numbering it '(3)', not
            # by naming a path
          - key: Y
            description: >-
              Coordinates Y1~Y4
            type: array
            items:
              type: number
            minItems: 4
            maxItems: 4
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'AREALOAD' by numbering it '(4)', not
            # by naming a path
          - key: LOAD
            description: >-
              Load (Uniform: [F1] / Trap: [F1,F2,F3,F4])
            type: array
            items:
              type: number
            minItems: 4
            maxItems: 4
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'AREALOAD' by numbering it '(5)', not
            # by naming a path

sdkRules:
  - id: db-pnld-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-pnld-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 06_DB_Static_Loads.md line 971
  table: 파라미터
