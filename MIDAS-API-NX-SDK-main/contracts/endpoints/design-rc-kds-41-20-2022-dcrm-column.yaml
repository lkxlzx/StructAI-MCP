# /DESIGN/RC/KDS-41-20-2022/DCRM-COLUMN
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-dcrm-column
endpoint: /DESIGN/RC/KDS-41-20-2022/DCRM-COLUMN
name: Design Criteria for Rebars by Column Member
surface:
  className: RebarDesignCriteriaByColumnMember
  exportName: rebarDesignCriteriaByColumnMember
  modulePath: [design, rcKds, rebar]
  payloadTypeName: RebarDesignCriteriaByColumnMemberPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "30. `DESIGN/RC/KDS-41-20-2022/DCRM-COLUMN` — Design Criteria for Rebars by Column Member (기둥 부재별 철근 설계기준)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      부재 ID 문자열을 키로 갖는 맵
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: MAIN_REBAR
    description: >-
      주철근 규격 · 19종 (D4 ~ D57)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: TIES_SPIRALS
    description: >-
      띠철근/나선철근 규격 · 19종 (D4 ~ D57)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ARRANGEMENT_Y
    description: >-
      띠철근 다리 수 (local Y) · 2 ~ 20
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ARRANGEMENT_Z
    description: >-
      띠철근 다리 수 (local Z) · 2 ~ 20
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DO
    description: >-
      주철근 중심까지 피복 거리 do
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SPACING_LIMIT
    description: >-
      철근 간격 제한 고려
    type: boolean
    requirement: optional
    documentedDefault: true
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SPLICED_BARS
    description: >-
      이음 옵션 (None | 50% | 100%)
    type: string
    requirement: optional
    documentedDefault: "50%"
    documentedOptional: true
    enum: ["None", "50%", "100%"]
    safeToOmit: unverified
    provenance: manual
    provenanceNote: >-
      Transcribed from the Specifications row the extractor used to drop:
      its Description cell writes the three options with GFM-escaped pipes
      (`None \| 50% \| 100%`), the row was split on those pipes into more
      cells than the header has, and a row whose cell count disagrees was
      discarded. The section's own JSON Schema declares SPLICED_BARS, which
      is how the loss was found - see MD-10.

extraction:
  source: 26_Design_RC_KDS41202022.md line 5242
  table: 파라미터
