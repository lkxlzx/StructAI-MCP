# /db/LLANch
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-llanch
endpoint: /db/LLANch
name: Traffic Line Lanes – China
surface:
  className: TrafficLineLanesChina
  exportName: trafficLineLanesChina
  modulePath: [db, movingLoads]
  payloadTypeName: TrafficLineLanesChinaPayload
  nestedTypes:
    - {path: COMMON, name: LineLaneCommon, namespace: DbMovingLoadsTypes}
    - {path: LANE_ITEMS, name: LineLaneChinaItem, namespace: DbMovingLoadsTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 08_DB_Moving_Loads.md
    section: 3. /db/LLANch – Traffic Line Lanes – China
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    variant: per_id
    path: /db/LLANch/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: COMMON
    description: >-
      Common lane properties
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    # RESOLVED: stated elsewhere in the same section - the Request Example
    # and the Python example both nest these ten under COMMON. The members
    # and their descriptions are /db/LLAN's, which is what this section
    # points at rather than restating. Until 2026-09-04 this contract
    # published its LANE_ITEMS rows at the root and had neither key.
    properties:
      - key: LL_NAME
        description: >-
          Name of Line Lane
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: WIDTH
        description: >-
          Lane Width
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: WHEEL_SPACE
        description: >-
          Wheel Spacing
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: OPT_AUTO_LANE
        description: >-
          Transverse Lane Optimization
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: ALLOW_WIDTH
        description: >-
          Allow Width for Optimization
        type: number
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: LOAD_DIST
        description: >-
          Load Distribution ("LANE" / "CROSS")
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: GROUP_NAME
        description: >-
          Name of Structure Group
        type: string
        requirement: optional
        documentedDefault: ""
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: SKEW_START
        description: >-
          Skew Start
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: SKEW_END
        description: >-
          Skew End
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: MOVING
        description: >-
          Moving Direction ("FORWARD" / "BACKWARD" / "BOTH")
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
  - key: LANE_ITEMS
    description: >-
      Lane Items
    type: array
    items:
      type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    # RESOLVED: stated elsewhere in the same section - this section's one
    # parameter table is headed "Parameters - LANE_ITEMS" and describes
    # these members, not the record's own. extraction.table has recorded
    # that heading since the draft was made.
    properties:
      - key: ELEM
        description: >-
          Element No.
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: ECC
        description: >-
          Eccentricity
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: SPAN
        description: >-
          Span Length
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: SPAN_START
        description: >-
          Span Start
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: SCALE_FACTOR
        description: >-
          Scale Factor
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
  - key: SPECIAL_LANE_ITEMS
    description: >-
      Used only when importing
    type: array
    items:
      type: object
    requirement: unstated
    documentedDefault: null
    documentedOptional: null
    safeToOmit: unverified
    provenance: info_schema
    # RESOLVED: declared by /info and absent from the manual - the whole
    # manual was searched for the name, not just this section.
    # The server's own description is the whole of what is known about
    # when this applies. No rule is written from it.
    properties:
      - key: ECC
        description: >-
          Eccentricity
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: info_schema
      - key: ELEMS
        description: >-
          Key Elements
        type: array
        items:
          type: integer
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: info_schema
      - key: SCALE_FACTOR
        description: >-
          Scale Factor
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: info_schema
      - key: SPAN
        description: >-
          Span
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: info_schema
      - key: SPAN_START
        description: >-
          Span Start
        type: boolean
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: info_schema
      - key: SPAN_START_NO_LIST
        description: >-
          Span Start No
        type: array
        items:
          type: integer
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: info_schema

manualDefects:
  - describes: field_name
    manualSays: >-
      /db/LLANch's record is `COMMON` and `LANE_ITEMS`.
    actual: >-
      `GET /info/db/LLANch` declares a further property,
      `SPECIAL_LANE_ITEMS`, an array of {`ECC`, `ELEMS`, `SCALE_FACTOR`,
      `SPAN`, `SPAN_START`, `SPAN_START_NO_LIST`} described " Used only when
      importing". That description is the whole of what is known about when
      it applies; no contract rule is written from it. The name appears
      nowhere in the manual - the whole repo was searched, not just this
      section.
    evidence: >-
      GET /info/db/LLANch on Civil NX, captured 2026-09-03, against
      docs/manual/08_DB_Moving_Loads.md. The same undocumented array is on
      /db/LLANtr, where it was first registered as MD-45; this is that
      finding reaching its siblings.
sdkRules:
  - id: db-llanch-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-llanch-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 08_DB_Moving_Loads.md line 297
  table: Parameters – LANE_ITEMS
  structuralTables:
    - heading: Parameters – LANE_ITEMS
      line: 296
      paths: [LANE_ITEMS]
      note: >-
        The section's only parameter table, and its heading names where its
        five rows go. Until 2026-09-04 the contract published them at the
        root, which made its npm payload type describe a record neither the
        manual's examples nor GET /info/db/LLANch recognise.
  prose:
    - line: 294
      quote: >-
        LLAN COMMON과 동일 구조 (`"LL_NAME"`, `"LOAD_DIST"`, `"MOVING"` 등)
      paths: [COMMON, COMMON.LL_NAME, COMMON.WIDTH, COMMON.WHEEL_SPACE,
              COMMON.OPT_AUTO_LANE, COMMON.ALLOW_WIDTH, COMMON.LOAD_DIST,
              COMMON.GROUP_NAME, COMMON.SKEW_START, COMMON.SKEW_END,
              COMMON.MOVING]
