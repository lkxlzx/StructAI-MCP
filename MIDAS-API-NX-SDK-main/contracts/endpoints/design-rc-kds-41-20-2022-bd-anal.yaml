# /DESIGN/RC/KDS-41-20-2022/BD-ANAL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-bd-anal
endpoint: /DESIGN/RC/KDS-41-20-2022/BD-ANAL
name: RC Beam Design Perform

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "39. `DESIGN/RC/KDS-41-20-2022/BD-ANAL` — RC Beam Design Perform (RC 보 설계 수행)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: performBeamDesign
      modulePath: [design, rcKds, designForces]
      argumentTypeName: RcMemberDesignPerformArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #39 — BD-ANAL — RC Beam Design
        Perform. Executes the RC beam design calculation; results are stored on
        the model and later retrieved via BD-TABLE/BD-REPORT. Response:
        ``{"message": "success"}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PERFORM_TYPE
    description: >-
      수행 대상 ("ALL"=전체, "ELEMS"=요소번호, "SECTIONS"=단면번호)
    type: string
    requirement: optional
    documentedDefault: ALL
    documentedOptional: true
    enum: [ALL, ELEMS, SECTIONS]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 지정 (ELEMS/SECTIONS 중 하나) — KEYS/TO/STRUCTURE_GROUP_NAME 중 하나
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          요소 ID 각각 지정
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.1', not by
        # naming a path
      - key: TO
        description: >-
          요소 ID 범위 (예 "1to160")
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.2', not by
        # naming a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹명
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.3', not by
        # naming a path
  - key: SECTIONS
    description: >-
      단면 번호 목록 (ELEMS/SECTIONS 중 하나)
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 7287
  table: 파라미터
