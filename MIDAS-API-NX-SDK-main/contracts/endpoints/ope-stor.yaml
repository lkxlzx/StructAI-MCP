# /ope/STOR
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: ope-stor
endpoint: /ope/STOR
name: Story Calculation

products: [gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 15_OPE.md
    section: "9. `/ope/STOR` — Story Calculation"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_gen
  records:
    - product: gen
      ref: db-write-sweep-2026-07-30

operations:
  - method: POST
    surface:
      exportName: calculateStory
      modulePath: [ope]
      argumentTypeName: StoryCalculationArgument
      documentation: |-
        docs/manual/15_OPE.md #9 — /ope/STOR — Story Calculation.

        ⚠️ Confirmed 2026-08-11: 404s on Civil NX (v2.2, build 08/11/2026),
        both `SEIS_ECC`/`WIND_ECC` disabled, on a blank document — first
        direct test of this endpoint against Civil. Matches the same
        Gen-only pattern already confirmed for the rest of the story family
        (`STORY_PARAM`, `STORY_IRR_PARAM`, `STORYPROP`, and the `/post/TABLE`
        story-table types) — the whole story feature set looks to be
        unregistered on Civil, not just this one endpoint.
      nestedTypes:
        - {path: SEIS_ECC, name: SeismicAccidentalEccentricity, namespace: OpeTypes}
        - {path: WIND_ECC, name: WindAccidentalEccentricity, namespace: OpeTypes}
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: SEIS_ECC
    description: >-
      지진 우발편심
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: INC_SEIS_ECC
        description: >-
          지진 우발편심 포함 여부
        type: boolean
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: SEIS_ECC_VALUE
        description: >-
          지진 우발편심 값(%)
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
  - key: WIND_ECC
    description: >-
      풍하중 편심
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: INC_WIND_ECC
        description: >-
          풍하중 편심 포함 여부
        type: boolean
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: WIND_ECC_VALUE
        description: >-
          풍하중 편심 값(%)
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual

extraction:
  source: 15_OPE.md line 1477
  table: Parameters
