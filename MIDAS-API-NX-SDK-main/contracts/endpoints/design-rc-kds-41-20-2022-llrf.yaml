# /DESIGN/RC/KDS-41-20-2022/LLRF
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-llrf
endpoint: /DESIGN/RC/KDS-41-20-2022/LLRF
name: Live Load Reduction Factor
surface:
  className: LiveLoadReductionFactor
  exportName: liveLoadReductionFactor
  modulePath: [design, rcKds, setup]
  payloadTypeName: LiveLoadReductionFactorPayload
  nestedTypes:
    - {path: REDUCTION_DATA, name: LiveLoadReductionDataItem, namespace: DesignRcKdsSetupTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "3. `DESIGN/RC/KDS-41-20-2022/LLRF` — Live Load Reduction Factor (활하중 저감계수)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼 (ID 문자열 키)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: CALC_RULE
    description: >-
      계산 규칙 (0=일반 설계코드, 1=중국 기준)
    type: integer
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    enum: [0, 1]
    safeToOmit: unverified
    provenance: manual
  - key: APPLIED_COMP
    description: >-
      적용 성분 (ALL=전체, AXIAL=축력, MOMENTS=모멘트, SHEAR=전단)
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: ["AXIAL"]
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: LIVE_LOAD_CASES
    description: >-
      대상 활하중 케이스명 목록
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: REDUCTION_DATA
    description: >-
      활하중 저감계수 테이블 데이터
    type: array
    items:
      type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: STORY
        description: >-
          층 이름
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.1', not by naming a path
      - key: XMIN
        description: >-
          X 최소 좌표
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.2', not by naming a path
      - key: XMAX
        description: >-
          X 최대 좌표
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.3', not by naming a path
      - key: YMIN
        description: >-
          Y 최소 좌표
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.4', not by naming a path
      - key: YMAX
        description: >-
          Y 최대 좌표
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.5', not by naming a path
      - key: RANGE_MAX
        description: >-
          최대값 Rmax (1~0.5, CALC_RULE=0일 때)
        type: number
        requirement: optional
        documentedDefault: 1
        documentedOptional: true
        enum: [1, 0.95, 0.9, 0.85, 0.8, 0.75, 0.7, 0.65, 0.6, 0.55, 0.5]
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.6', not by naming a path
      - key: RANGE_MIN
        description: >-
          최소값 Rmin (1~0.5, CALC_RULE=0일 때)
        type: number
        requirement: optional
        documentedDefault: 0.5
        documentedOptional: true
        enum: [1, 0.95, 0.9, 0.85, 0.8, 0.75, 0.7, 0.65, 0.6, 0.55, 0.5]
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'REDUCTION_DATA' by numbering it
        # '5.7', not by naming a path

extraction:
  source: 26_Design_RC_KDS41202022.md line 1056
  table: 파라미터
