# /DESIGN/RC/KDS-41-20-2022/BEMW
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-bemw
endpoint: /DESIGN/RC/KDS-41-20-2022/BEMW
name: Boundary Element Method by Wall ID
surface:
  className: BoundaryElementMethodByWallId
  exportName: boundaryElementMethodByWallId
  modulePath: [design, rcKds, rebar]
  payloadTypeName: BoundaryElementMethodByWallIdPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "26. `DESIGN/RC/KDS-41-20-2022/BEMW` — Boundary Element Method by Wall ID (벽체ID별 경계요소법)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼 (인덱스 문자열 키, 1개)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: BBNDR_ELEM_METHOD
    description: >-
      경계요소법 사용 여부
    type: boolean
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: BBOT_STOR
    description: >-
      최하층 설정 사용 여부
    type: boolean
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: NMETHOD_TYPE
    description: >-
      방식 타입 (Displacement Based Method / Stress Based Method) —
      BBNDR_ELEM_METHOD=true
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    enum: [Displacement Based Method, Stress Based Method]
    safeToOmit: unverified
    provenance: manual
  - key: STOR_NAME
    description: >-
      층 이름 (BBOT_STOR=true일 때)
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 4734
  table: 파라미터
