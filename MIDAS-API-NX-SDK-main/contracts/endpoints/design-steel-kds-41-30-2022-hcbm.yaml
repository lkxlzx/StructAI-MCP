# /DESIGN/STEEL/KDS-41-30-2022/HCBM
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-hcbm
endpoint: /DESIGN/STEEL/KDS-41-30-2022/HCBM
name: Haunched Beam Assignment
surface:
  className: HaunchedBeamAssignment
  exportName: haunchedBeamAssignment
  modulePath: [design, steelKds]
  payloadTypeName: HaunchedBeamAssignmentPayload
  nestedTypes:
    - {path: PART_A, name: HaunchPartSelector, namespace: DesignSteelKdsTypes}
    - {path: PART_B, name: HaunchPartSelector, namespace: DesignSteelKdsTypes}
    - {path: PART_C, name: HaunchPartSelector, namespace: DesignSteelKdsTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "11. `DESIGN/STEEL/KDS-41-30-2022/HCBM` — Haunched Beam Assignment (헌치 보 배정)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: NAME
    description: >-
      헌치 이름
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PART_A
    description: >-
      Part A 요소 입력 (방법 1개만 사용)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: INPUT_METHOD
        description: >-
          입력 방법 ("KEYS"=개별 ID, "TO"=범위)
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        enum: [KEYS, TO]
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PART_A' by numbering it '3.1', not by
        # naming a path
      - key: KEYS
        description: >-
          개별 요소 ID (INPUT_METHOD=KEYS, minItems 1)
        type: array
        items:
          type: integer
        minItems: 1
        requirement: conditional
        condition: "INPUT_METHOD=\"KEYS\""
        appliesWhen:
          - path: PART_A.INPUT_METHOD
            equals: KEYS
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PART_A' by numbering it '3.2', not by
        # naming a path
      - key: TO
        description: >-
          ID 범위 문자열 (INPUT_METHOD=TO, 예 "101 to 105")
        type: string
        requirement: conditional
        condition: "INPUT_METHOD=\"TO\""
        appliesWhen:
          - path: PART_A.INPUT_METHOD
            equals: TO
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PART_A' by numbering it '3.3', not by
        # naming a path
  - key: PART_B
    description: >-
      Part B 요소 입력 (구조는 PART_A와 동일)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: INPUT_METHOD
        description: >-
          입력 방법 ("KEYS"=개별 ID, "TO"=범위)
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        enum: [KEYS, TO]
        safeToOmit: unverified
        provenance: manual
      - key: KEYS
        description: >-
          개별 요소 ID (INPUT_METHOD=KEYS, minItems 1)
        type: array
        items:
          type: integer
        minItems: 1
        requirement: conditional
        condition: "INPUT_METHOD=\"KEYS\""
        appliesWhen:
          - path: PART_B.INPUT_METHOD
            equals: KEYS
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: TO
        description: >-
          ID 범위 문자열 (INPUT_METHOD=TO, 예 "101 to 105")
        type: string
        requirement: conditional
        condition: "INPUT_METHOD=\"TO\""
        appliesWhen:
          - path: PART_B.INPUT_METHOD
            equals: TO
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
  - key: PART_C
    description: >-
      Part C 요소 입력 (구조는 PART_A와 동일)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: INPUT_METHOD
        description: >-
          입력 방법 ("KEYS"=개별 ID, "TO"=범위)
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        enum: [KEYS, TO]
        safeToOmit: unverified
        provenance: manual
      - key: KEYS
        description: >-
          개별 요소 ID (INPUT_METHOD=KEYS, minItems 1)
        type: array
        items:
          type: integer
        minItems: 1
        requirement: conditional
        condition: "INPUT_METHOD=\"KEYS\""
        appliesWhen:
          - path: PART_C.INPUT_METHOD
            equals: KEYS
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: TO
        description: >-
          ID 범위 문자열 (INPUT_METHOD=TO, 예 "101 to 105")
        type: string
        requirement: conditional
        condition: "INPUT_METHOD=\"TO\""
        appliesWhen:
          - path: PART_C.INPUT_METHOD
            equals: TO
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
  - key: POS_TYPE
    description: >-
      설계 위치 타입 (0=Part 1/2, 1=User)
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [0, 1]
    safeToOmit: unverified
    provenance: manual
  - key: L1
    description: >-
      사용자 정의 L1 거리 (POS_TYPE=1)
    type: number
    requirement: optional
    documentedDefault: 1
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: L2
    description: >-
      사용자 정의 L2 거리 (POS_TYPE=1)
    type: number
    requirement: optional
    documentedDefault: 1
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 2074
  table: 파라미터
