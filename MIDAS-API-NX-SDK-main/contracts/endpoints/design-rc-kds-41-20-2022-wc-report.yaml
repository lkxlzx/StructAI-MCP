# /DESIGN/RC/KDS-41-20-2022/WC-REPORT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-wc-report
endpoint: /DESIGN/RC/KDS-41-20-2022/WC-REPORT
name: RC Wall Check Report

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "65. `DESIGN/RC/KDS-41-20-2022/WC-REPORT` — RC Wall Check Report (RC 벽체 검토 리포트)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: exportWallCheckReport
      modulePath: [design, rcKds, checks]
      argumentTypeName: RcWallCheckReportArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #65 — WC-REPORT — RC Wall
        Check Report. Response: ``{"SUCCESS": bool, "FILE_PATH": str, "MESSAGE":
        str}``.
      nestedTypes:
        - {path: SELECTIONS, name: RcWallDesignSelection, namespace: DesignRcKdsDesignForcesTypes}
        - {path: SELECTIONS.WALL_IDS, name: RcWallIdsSelector, namespace: DesignRcKdsDesignForcesTypes}
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: REPORT_TYPE
    description: >-
      출력 단위 ("WID+STORY" / "WID")
    type: string
    requirement: required
    documentedDefault: WID+STORY
    documentedOptional: false
    enum: [WID+STORY, WID]
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE_WID_STORY
    description: >-
      WID+STORY 모드 (Graphic/Detail/Summary/PMCurve)
    type: string
    requirement: conditional
    condition: "REPORT_TYPE=\"WID+STORY\""
    appliesWhen:
      - path: REPORT_TYPE
        equals: WID+STORY
    documentedDefault: null
    documentedOptional: false
    enum: [Graphic, Detail, Summary, PMCurve]
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE_WID
    description: >-
      WID 모드 (Graphic/Summary/PMCurve, Detail 미지원)
    type: string
    requirement: conditional
    condition: "REPORT_TYPE=\"WID\""
    appliesWhen:
      - path: REPORT_TYPE
        equals: WID
    documentedDefault: null
    documentedOptional: false
    enum: [Graphic, Summary, PMCurve]
    safeToOmit: unverified
    provenance: manual
  - key: SELECTIONS
    description: >-
      벽/층 선택 목록 (생략 시 전체)
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: WALL_IDS
        description: >-
          벽 ID (KEYS 또는 TO)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'SELECTIONS' by numbering it '5.1',
        # not by naming a path
        properties:
          - key: KEYS
            description: >-
              벽 ID 각각
            type: array
            items:
              type: integer
            requirement: optional
            documentedDefault: null
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # From this section's JSON Schema (line 12323), which declares KEYS and TO
            # under WALL_IDS with a oneOf: exactly one of the two. The Parameters table
            # names them only in WALL_IDS's description.
          - key: TO
            description: >-
              벽 ID 범위 (예 "10to20")
            type: string
            requirement: optional
            documentedDefault: null
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
      - key: STORY
        description: >-
          층 이름 목록
        type: array
        items:
          type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'SELECTIONS' by numbering it '5.2',
        # not by naming a path
  - key: EXPORT_PATH
    description: >-
      리포트 저장 디렉터리 경로
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: OUTPUT_NAME
    description: >-
      출력 파일 기본 이름
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  prose:
    - line: 12323
      quote: >-
        "KEYS": { "type": "array", "items": { "type": "integer" } },
        "TO": { "type": "string" }
      paths: [SELECTIONS.WALL_IDS.KEYS, SELECTIONS.WALL_IDS.TO]
  source: 26_Design_RC_KDS41202022.md line 12378
  table: 파라미터
