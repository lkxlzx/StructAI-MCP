# /DESIGN/STEEL/KDS-41-30-2022/DCTL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-dctl
endpoint: /DESIGN/STEEL/KDS-41-30-2022/DCTL
name: Definition of Frame
surface:
  className: DefinitionOfFrame
  exportName: definitionOfFrame
  modulePath: [design, steelKds]
  payloadTypeName: DefinitionOfFramePayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "2. `DESIGN/STEEL/KDS-41-30-2022/DCTL` — Definition of Frame (프레임 정의)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼 (ID 1개만 허용, maxProperties=1)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: FRAMEX
    description: >-
      X방향 프레임 ("Unbraced Sway"=비횡지지·Sway, "Braced Non-sway"=횡지지·Non-sway)
    type: string
    requirement: optional
    documentedDefault: Braced Non-sway
    documentedOptional: true
    enum: [Unbraced Sway, Braced Non-sway]
    safeToOmit: unverified
    provenance: manual
  - key: FRAMEY
    description: >-
      Y방향 프레임 (값은 FRAMEX와 동일)
    type: string
    requirement: optional
    documentedDefault: Braced Non-sway
    documentedOptional: true
    enum: [Unbraced Sway, Braced Non-sway]
    safeToOmit: unverified
    provenance: manual
  - key: bAUTOKF
    description: >-
      유효좌굴길이계수 자동계산
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: DT
    description: >-
      설계 타입 ("3D"=3-D, "XZ"=X-Z평면, "YZ"=Y-Z평면, "XY"=X-Y평면)
    type: string
    requirement: optional
    documentedDefault: 3D
    documentedOptional: true
    enum: [3D, XZ, YZ, XY]
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 508
  table: 파라미터
