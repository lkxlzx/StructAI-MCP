# /DESIGN/RC/KDS-41-20-2022/BD-REPORT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-bd-report
endpoint: /DESIGN/RC/KDS-41-20-2022/BD-REPORT
name: RC Beam Design Report

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "41. `DESIGN/RC/KDS-41-20-2022/BD-REPORT` — RC Beam Design Report (RC 보 설계 리포트)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: exportBeamDesignReport
      modulePath: [design, rcKds, designForces]
      argumentTypeName: RcBeamDesignReportArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #41 — BD-REPORT — RC Beam
        Design Report. Response: ``{"SUCCESS": bool, "FILE_PATH": str,
        "MESSAGE": str}``.
      nestedTypes:
        - {path: DETAIL_POSITIONS, name: RcReportDetailPositions, namespace: DesignRcKdsDesignForcesTypes}
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: REPORT_TYPE
    description: >-
      리포트 타입 ("MEMB"=부재별, "PROP"=단면별)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [MEMB, PROP]
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE_MEMB
    description: >-
      출력 모드 (부재별) — "Graphic"/"Detail"/"Summary"
    type: string
    requirement: conditional
    condition: MEMB
    documentedDefault: null
    documentedOptional: false
    enum: [Graphic, Detail, Summary]
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE_PROP
    description: >-
      출력 모드 (단면별) — "Graphic"/"Summary"
    type: string
    requirement: conditional
    condition: PROP
    documentedDefault: null
    documentedOptional: false
    enum: [Graphic, Summary]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 지정 (ELEMS/SECTIONS 중 하나)
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SECTIONS
    description: >-
      단면 번호 목록
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DETAIL_POSITIONS
    description: >-
      Detail 출력 위치 (END_I/MID/END_J) — Detail 모드일 때
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: END_I
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: MID
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: END_J
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
    # Members from this section's JSON Schema (line 7575); the
    # Parameters table names the object without child rows.
  - key: EXPORT_PATH
    description: >-
      저장 디렉터리 경로 (예 C:\\MIDAS\\Report\\)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: OUTPUT_NAME
    description: >-
      출력 파일 기본 이름 (다중 요소 시 인덱스·요소번호 접두어 부가)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  prose:
    - line: 7575
      quote: >-
        "DETAIL_POSITIONS": { ... (JSON Schema)
      paths: [DETAIL_POSITIONS.END_I, DETAIL_POSITIONS.MID, DETAIL_POSITIONS.END_J]
  source: 26_Design_RC_KDS41202022.md line 7592
  table: 파라미터
