# /db/ETFC
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-etfc
endpoint: /db/ETFC
name: Ambient Temperature Functions
surface:
  className: AmbientTemperatureFunction
  exportName: ambientTemperatureFunction
  modulePath: [db, constructionStage]
  payloadTypeName: AmbientTemperatureFunctionPayload
  nestedTypes:
    - {path: ITEM, name: TimeValuePoint, namespace: DbBaseTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 10_DB_Construction_Stage.md
    section: 7. /db/ETFC – Ambient Temperature Functions
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-17
    - product: gen
      ref: db-write-sweep-2026-08-17

operations:
  - method: DELETE
    variant: per_id
    path: /db/ETFC/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      함수명
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: TYPE
    description: >-
      함수 타입 ("CONST" / "SINE" / "USER")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: TEMP
    description: >-
      온도
    type: number
    requirement: optional
    condition: "Constant 타입 (TYPE=\"CONST\") 추가 파라미터"
    appliesWhen:
      - path: TYPE
        equals: CONST
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: MAX_TEMP
    description: >-
      최대 온도 (T)
    type: number
    requirement: optional
    condition: "Sine 타입 (TYPE=\"SINE\") 추가 파라미터"
    appliesWhen:
      - path: TYPE
        equals: SINE
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's AmbientTemperatureFunction case completed a
      live create-read-update-delete round trip on gen and civil without this
      field in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: MEAN_TEMP
    description: >-
      평균 온도 (To)
    type: number
    requirement: optional
    condition: "Sine 타입 (TYPE=\"SINE\") 추가 파라미터"
    appliesWhen:
      - path: TYPE
        equals: SINE
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's AmbientTemperatureFunction case completed a
      live create-read-update-delete round trip on gen and civil without this
      field in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: DELAY_TIME
    description: >-
      지연 시간 (to)
    type: number
    requirement: optional
    condition: "Sine 타입 (TYPE=\"SINE\") 추가 파라미터"
    appliesWhen:
      - path: TYPE
        equals: SINE
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's AmbientTemperatureFunction case completed a
      live create-read-update-delete round trip on gen and civil without this
      field in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: SCALE_FACTOR
    description: >-
      스케일 계수
    type: number
    requirement: required
    condition: "User 타입 (TYPE=\"USER\") 추가 파라미터"
    appliesWhen:
      - path: TYPE
        equals: USER
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's AmbientTemperatureFunction case completed a
      live create-read-update-delete round trip on gen and civil without this
      field in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: ITEM
    description: >-
      함수 데이터 목록
    type: array
    items:
      type: object
    requirement: required
    condition: "User 타입 (TYPE=\"USER\") 추가 파라미터"
    appliesWhen:
      - path: TYPE
        equals: USER
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's AmbientTemperatureFunction case completed a
      live create-read-update-delete round trip on gen and civil without this
      field in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
    properties:
      - key: TIME
        description: >-
          - 시간
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEM' by a dash description row
      - key: VALUE
        description: >-
          - 온도
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEM' by a dash description row

sdkRules:
  - id: db-etfc-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-etfc-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 10_DB_Construction_Stage.md line 755
  table: 7-2. 파라미터
