# /db/HHCT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-hhct
endpoint: /db/HHCT
name: Heat of Hydration Analysis Control
surface:
  className: HeatOfHydrationAnalysisControl
  exportName: heatOfHydrationAnalysisControl
  modulePath: [db, analysisControl]
  payloadTypeName: HeatOfHydrationAnalysisControlPayload
  nestedTypes:
    - {path: ITEM, name: CreepShrinkageItem, namespace: DbAnalysisControlTypes}
    - {path: ITEM.M_EFF_MOD, name: CreepShrinkageEffectiveModulusData, namespace: DbAnalysisControlTypes}
    - {path: ITEM.M_GENERAL, name: CreepShrinkageGeneralData, namespace: DbAnalysisControlTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 7. /db/HHCT — Heat of Hydration Analysis Control
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-31
    - product: gen
      ref: db-write-sweep-2026-08-31

operations:
  - method: DELETE
    variant: per_id
    path: /db/HHCT/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: FINAL_STAGE
    description: >-
      Final Stage (Last Stage: true / Other Stage: false)
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: STAGE_NAME
    description: >-
      Construction Stage for Hydration (when FINAL_STAGE false)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: THETA
    description: >-
      Integration Factor
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: INIT_TEMP
    description: >-
      Initial Temperature
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: EVAL
    description: >-
      Element Stress Evaluation ("CENTER" / "GAUSS" / "NODAL")
    type: string
    requirement: optional
    documentedDefault: CENTER
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: OPT_IS_CREEP_SHRINKAGE
    description: >-
      Creep & Shrinkage Option
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ITEM
    description: >-
      Creep & Shrinkage 설정 객체
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: TYPE
        description: >-
          Type (Creep: "CREEP" / Shrinkage: "SHRINK" / Both: "BOTH")
        type: string
        requirement: optional
        documentedDefault: CREEP
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEM' by numbering it '(1)', not
        # by naming a path
      - key: CREEP_CALC_METHOD
        description: >-
          Creep Calculation Method (General: 0 / Effective Modulus: 1)
        type: integer
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEM' by numbering it '(2)', not
        # by naming a path
      - key: M_GENERAL
        description: >-
          General Data (when method 0)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEM' by numbering it '(3)', not
        # by naming a path
        properties:
          - key: ITER
            description: >-
              Number of Iterations
            type: integer
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'M_GENERAL' by numbering it 'i',
            # not by naming a path
          - key: TOL
            description: >-
              Tolerance
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'M_GENERAL' by numbering it 'ii',
            # not by naming a path
      - key: M_EFF_MOD
        description: >-
          Effective Modulus Data (when method 1)
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEM' by numbering it '(3)', not
        # by naming a path
        properties:
          - key: PHI1
            description: >-
              Phi1
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'M_EFF_MOD' by numbering it 'i',
            # not by naming a path
          - key: DAY1
            description: >-
              Day1
            type: integer
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'M_EFF_MOD' by numbering it 'ii',
            # not by naming a path
          - key: PHI2
            description: >-
              Phi2
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'M_EFF_MOD' by numbering it 'iii',
            # not by naming a path
          - key: DAY2
            description: >-
              Day2
            type: integer
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'M_EFF_MOD' by numbering it 'iv',
            # not by naming a path
  - key: OPT_USE_EQUI_AGE
    description: >-
      Use Equivalent Age by Time & Temperature
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: OPT_INCL_SELF_WEIGHT
    description: >-
      Include Self-weight Load
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SELF_WEIGHT_FACTOR
    description: >-
      Self-weight Factor
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

sdkRules:
  - id: db-hhct-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-hhct-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 714
  table: Parameters
