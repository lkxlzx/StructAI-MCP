# /DESIGN/RC/KDS-41-20-2022/REBW
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-rebw
endpoint: /DESIGN/RC/KDS-41-20-2022/REBW
name: Modify Wall Rebar Data
surface:
  className: ModifyWallRebarData
  exportName: modifyWallRebarData
  modulePath: [design, rcKds, rebar]
  payloadTypeName: ModifyWallRebarDataPayload
  nestedTypes:
    - {path: ITEMS, name: RcWallRebarItem, namespace: DesignRcKdsRebarTypes}
    - {path: ITEMS.BE_HORIZONTAL_REBAR, name: RebarNameDist, namespace: DesignRcKdsRebarTypes}
    - {path: ITEMS.CONCRETE_FACE_TO_CENTER_OF_REBAR, name: RcWallConcreteFaceToCenterOfRebar, namespace: DesignRcKdsRebarTypes}
    - {path: ITEMS.END_REBAR, name: RcWallEndRebarSpec, namespace: DesignRcKdsRebarTypes}
    - {path: ITEMS.HORIZONTAL_REBAR, name: RebarNameDist, namespace: DesignRcKdsRebarTypes}
    - {path: ITEMS.STORY, name: RcWallStoryRange, namespace: DesignRcKdsRebarTypes}
    - {path: ITEMS.VERTICAL_REBAR, name: RebarNameDist, namespace: DesignRcKdsRebarTypes}

products: [gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "37. `DESIGN/RC/KDS-41-20-2022/REBW` — Modify Wall Rebar Data (벽체 철근 데이터 수정)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_gen
  records:
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      벽체 ID 문자열을 키로 갖는 맵
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ITEMS
    description: >-
      벽체 철근 항목 (min 1)
    type: array
    items:
      type: object
    minItems: 1
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: CREATE_SUB_WALL_ID
        description: >-
          서브 벽체 ID 생성
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(1)', not by
        # naming a path
      - key: SUB_WALL_ID
        description: >-
          서브 벽체 ID (읽기 전용, 생성 시 필수)
        type: integer
        requirement: conditional
        condition: 읽기 전용, 생성 시 필수
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(2)', not by
        # naming a path
      - key: STORY
        description: >-
          층 범위 (FROM/TO, 생성 시 필수)
        type: object
        requirement: conditional
        condition: CREATE_SUB_WALL_ID=true
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(3)', not by
        # naming a path
        properties:
          - key: FROM
            description: >-
              시작 층
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: stated elsewhere in the same section - the
            # section's own JSON Schema declares this object's members
            # with their types and its own `required` list, and the "철근 하위
            # 객체 필드 요약" table at line 6906 names them again. The
            # Parameters row only lists them inside its description cell,
            # which is why the parser saw an object with nothing in it.
          - key: TO
            description: >-
              종료 층
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: VERTICAL_REBAR
        description: >-
          수직 철근 (NAME·DIST)
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(4)', not by
        # naming a path
        properties:
          - key: NAME
            description: >-
              수직 철근 규격 (전체 19종: D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # no enum is transcribed: the JSON Schema's `enum` lists five
            # sizes while its own description says 전체 19종: D4 ~ D57, so
            # the list is an abbreviation rather than the value set.
          - key: DIST
            description: >-
              수직 철근 간격
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: HORIZONTAL_REBAR
        description: >-
          수평 철근 (NAME·DIST)
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(5)', not by
        # naming a path
        properties:
          - key: NAME
            description: >-
              수평 철근 규격 (전체 19종: D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # no enum is transcribed: the JSON Schema's `enum` lists five
            # sizes while its own description says 전체 19종: D4 ~ D57, so
            # the list is an abbreviation rather than the value set.
          - key: DIST
            description: >-
              수평 철근 간격
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: USE_END_REBAR
        description: >-
          단부 철근 입력 사용
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(6)', not by
        # naming a path
      - key: END_REBAR
        description: >-
          단부 철근 (NAME·NUM·DIST, 사용 시 필수)
        type: object
        requirement: conditional
        condition: NAME·NUM·DIST, 사용 시 필수
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(7)', not by
        # naming a path
        properties:
          - key: NAME
            description: >-
              단부(수직) 철근 규격 (전체 19종: D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # no enum is transcribed: the JSON Schema's `enum` lists five
            # sizes while its own description says 전체 19종: D4 ~ D57, so
            # the list is an abbreviation rather than the value set.
          - key: NUM
            description: >-
              단부 철근 개수
            type: integer
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: DIST
            description: >-
              단부 철근 간격
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: BE_HORIZONTAL_REBAR
        description: >-
          경계요소 수평 철근 (NAME·DIST)
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(8)', not by
        # naming a path
        properties:
          - key: NAME
            description: >-
              경계요소 수평 철근 규격 (전체 19종: D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # no enum is transcribed: the JSON Schema's `enum` lists five
            # sizes while its own description says 전체 19종: D4 ~ D57, so
            # the list is an abbreviation rather than the value set.
          - key: DIST
            description: >-
              경계요소 수평 철근 간격
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: BOUNDARY_ELEMENT_LENGTH
        description: >-
          경계요소 길이
        type: number
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(9)', not by
        # naming a path
      - key: CONCRETE_FACE_TO_CENTER_OF_REBAR
        description: >-
          콘크리트면~철근중심 거리 (DW·DE)
        type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(10)', not by
        # naming a path
        properties:
          - key: DW
            description: >-
              콘크리트면~수직 철근중심 (dw)
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: DE
            description: >-
              콘크리트면~경계요소 철근중심 (de)
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
      - key: USE_MODEL_THICKNESS
        description: >-
          모델 두께 사용
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(11)', not by
        # naming a path
      - key: THICKNESS
        description: >-
          벽체 두께 (USE_MODEL_THICKNESS=false 일 때 필수)
        type: number
        requirement: conditional
        condition: USE_MODEL_THICKNESS=false
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(12)', not by
        # naming a path

extraction:
  source: 26_Design_RC_KDS41202022.md line 6888
  table: 파라미터
  prose:
    - line: 6910
      quote: >-
        | `VERTICAL_REBAR` / `HORIZONTAL_REBAR` / `BE_HORIZONTAL_REBAR` |
        `NAME` (19종 D4 ~ D57) · `DIST` | 규격 · 간격 |
      paths: [ITEMS.VERTICAL_REBAR.NAME, ITEMS.VERTICAL_REBAR.DIST,
              ITEMS.HORIZONTAL_REBAR.NAME, ITEMS.HORIZONTAL_REBAR.DIST,
              ITEMS.BE_HORIZONTAL_REBAR.NAME, ITEMS.BE_HORIZONTAL_REBAR.DIST]
    - line: 6911
      quote: '| `END_REBAR` | `NAME` (19종) · `NUM` · `DIST` | 규격 · 개수 · 간격 |'
      paths: [ITEMS.END_REBAR.NAME, ITEMS.END_REBAR.NUM, ITEMS.END_REBAR.DIST]
    - line: 6912
      quote: >-
        | `CONCRETE_FACE_TO_CENTER_OF_REBAR` | `DW` · `DE` | 수직/경계요소 철근 피복 |
      paths: [ITEMS.CONCRETE_FACE_TO_CENTER_OF_REBAR.DW,
              ITEMS.CONCRETE_FACE_TO_CENTER_OF_REBAR.DE]
    - line: 6913
      quote: '| `STORY` | `FROM` · `TO` | 시작/종료 층 문자열 |'
      paths: [ITEMS.STORY.FROM, ITEMS.STORY.TO]
