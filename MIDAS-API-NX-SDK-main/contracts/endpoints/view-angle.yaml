# /view/ANGLE
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: view-angle
endpoint: /view/ANGLE
name: Viewpoint

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 16_VIEW.md
    section: "4. `/view/ANGLE` — Viewpoint"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-30
    - product: gen
      ref: db-read-sweep-2026-07-30

operations:
  - method: POST
    surface:
      exportName: setAngle
      modulePath: [view]
      argumentTypeName: AngleArgument
      documentation: |-
        docs/manual/16_VIEW.md #4 — /view/ANGLE — Viewpoint.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: HORIZONTAL
    description: >-
      수평 시점 각도
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: VERTICAL
    description: >-
      수직 시점 각도
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 16_VIEW.md line 389
  table: Parameters
