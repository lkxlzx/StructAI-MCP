# /DESIGN/STEEL/KDS-41-30-2022/CODE-REPORT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-code-report
endpoint: /DESIGN/STEEL/KDS-41-30-2022/CODE-REPORT
name: Steel Code Check Report

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "25. `DESIGN/STEEL/KDS-41-30-2022/CODE-REPORT` — Steel Code Check Report (강재 코드 검토 보고서)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: exportSteelCodeCheckReport
      modulePath: [design, steelKds]
      argumentTypeName: SteelCodeCheckReportArgument
      documentation: |-
        docs/manual/25_Design_Steel_KDS41302022.md #25 — CODE-REPORT — Steel
        Code Check Report. Response: ``{"SUCCESS": bool, "FILE_PATH": str,
        "MESSAGE": str}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: REPORT_TYPE
    description: >-
      보고서 표 타입 ("MEMB", "PROP")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [MEMB, PROP]
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE
    description: >-
      출력 모드 ("Graphic"=JPG, "Detail"=DOC, "Summary"=TXT)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [Graphic, Detail, Summary]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 입력 (ELEMS/SECTIONS 중 하나) — KEYS/TO/STRUCTURE_GROUP_NAME
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SECTIONS
    description: >-
      단면 번호 (ELEMS/SECTIONS 중 하나)
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      저장 디렉터리 경로 (예 C:\\MIDAS\\Report\\)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: OUTPUT_NAME
    description: >-
      출력 파일 기본 이름 (다중 요소 시 인덱스·요소번호 접두어 부가)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 5160
  table: 파라미터
