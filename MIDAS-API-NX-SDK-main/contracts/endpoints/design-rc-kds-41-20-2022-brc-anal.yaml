# /DESIGN/RC/KDS-41-20-2022/BRC-ANAL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-brc-anal
endpoint: /DESIGN/RC/KDS-41-20-2022/BRC-ANAL
name: RC Brace Check Perform

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "60. `DESIGN/RC/KDS-41-20-2022/BRC-ANAL` — RC Brace Check Perform (RC 가새 검토 수행)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: performBraceCheck
      modulePath: [design, rcKds, checks]
      argumentTypeName: PerformRcMemberCheckArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #60 — BRC-ANAL — RC Brace
        Check Perform. Runs P-M interaction/shear code-checking on RC brace
        members that already have rebar assigned. Response: ``{"message":
        "success"}``.

        ⚠️ Never independently tested. Its siblings ``perform_column_check``
        (CC-ANAL) and ``perform_beam_check`` (BC-ANAL) were confirmed to hang
        Gen NX 2026 (v2.1), build 06/23/2026, then ran clean on that same build
        on 2026-07-25 — i.e. the trigger is unidentified rather than fixed; see
        docs/live_verification_notes.md. Assume the same history applies here:
        use a short timeout, and read ``get_brace_check_table`` regardless of
        whether this call returns.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PERFORM_TYPE
    description: >-
      수행 대상 타입 ("ALL"=전체, "ELEMS"=요소별, "SECTIONS"=단면별)
    type: string
    requirement: optional
    documentedDefault: ALL
    documentedOptional: true
    enum: [ALL, ELEMS, SECTIONS]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 입력 (ELEMS / SECTIONS 중 하나)
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          개별 ID
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.1', not by
        # naming a path
      - key: TO
        description: >-
          ID 범위 (예 "1to160")
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.2', not by
        # naming a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹 이름
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.3', not by
        # naming a path
  - key: SECTIONS
    description: >-
      단면 번호 (ELEMS / SECTIONS 중 하나)
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 10945
  table: 파라미터
