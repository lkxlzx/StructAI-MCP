# /DESIGN/STEEL/KDS-41-30-2022/CODE-ANAL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-code-anal
endpoint: /DESIGN/STEEL/KDS-41-30-2022/CODE-ANAL
name: Steel Code Check Perform

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "23. `DESIGN/STEEL/KDS-41-30-2022/CODE-ANAL` — Steel Code Check Perform (강재 코드 검토 수행)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-30
    - product: gen
      ref: db-read-sweep-2026-07-30

operations:
  - method: POST
    surface:
      exportName: performSteelCodeCheck
      modulePath: [design, steelKds]
      argumentTypeName: PerformSteelCodeCheckArgument
      documentation: |-
        docs/manual/25_Design_Steel_KDS41302022.md #23 — CODE-ANAL — Steel Code
        Check Perform. Executes the design calculation; response is
        ``{"message": "success"}`` on success.

        ⚠️ Never independently tested. The RC equivalent
        (``design.rc_kds.checks.perform_column_check``, CC-ANAL) was confirmed
        to hang Gen NX 2026 (v2.1), build 06/23/2026 (stuck at "Converting
        Design Results... 0%", required a forced process kill), then ran clean
        on that same build on 2026-07-25 — the trigger is unidentified, not
        fixed; see docs/live_verification_notes.md. Every test on both sides
        used the **KDS 41 20:2022 RC** code, so nothing is confirmed either way
        for this steel-code path. Use a short timeout and read
        ``get_steel_code_check_table`` regardless of whether this call returns.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PERFORM_TYPE
    description: >-
      수행 대상 타입 ("ALL"=전체 요소, "ELEMS"=요소별, "SECTIONS"=단면별)
    type: string
    requirement: optional
    documentedDefault: ALL
    documentedOptional: true
    enum: [ALL, ELEMS, SECTIONS]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 입력 (ELEMS/SECTIONS 중 하나)
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          개별 ID
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.1', not by
        # naming a path
      - key: TO
        description: >-
          ID 범위 (예 "1to160")
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.2', not by
        # naming a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹 이름
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.3', not by
        # naming a path
  - key: SECTIONS
    description: >-
      단면 번호 (ELEMS/SECTIONS 중 하나)
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 4409
  table: 파라미터
