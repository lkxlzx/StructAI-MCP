# /db/EIGV-M1
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-eigv-m1
endpoint: /db/EIGV-M1
name: Eigenvalue Analysis Control (Hyper-S)
surface:
  className: EigenvalueAnalysisControlHyperS
  exportName: eigenvalueAnalysisControlHyperS
  modulePath: [db, analysisControl]
  payloadTypeName: EigenvalueAnalysisControlHyperSPayload
  nestedTypes:
    - {path: FREQ_RANGE, name: EigenvalueFrequencyRangeHyperS, namespace: DbAnalysisControlTypes}
    - {path: GLINK_VECTOR, name: EigenvalueGlinkVectorHyperS, namespace: DbAnalysisControlTypes}
    - {path: RITZ_LOAD, name: EigenvalueRitzLoadItemHyperS, namespace: DbAnalysisControlTypes}

products: [civil]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 6. /db/EIGV-M1 — Eigenvalue Analysis Control (Hyper-S)
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_civil
  records:
    - product: civil
      ref: db-write-sweep-2026-08-25

operations:
  - method: DELETE
    variant: per_id
    path: /db/EIGV-M1/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: ANAL_TYPE
    description: >-
      Eigen Vectors (Lanczos: "LANCZOS" / Ritz Vectors: "RITZ")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [LANCZOS, RITZ]
    safeToOmit: unverified
    provenance: manual

variants:
  - when:
      - path: ANAL_TYPE
        equals: LANCZOS
    source:
      table: "Parameters — Lanczos (`ANAL_TYPE = \"LANCZOS\"`)"
      line: 596
    fields:
      - key: FREQ_NO
        description: >-
          Number of Frequencies (1~1000)
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
      - key: FREQ_RANGE
        description: >-
          Frequency range of interest
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: OPT_USE
            description: >-
              Use Option
            type: boolean
            requirement: required
            documentedDefault: false
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'FREQ_RANGE' by numbering it '(1)',
            # not by naming a path
          - key: FREQ_MIN
            description: >-
              Search From (when OPT_USE true)
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'FREQ_RANGE' by numbering it '(2)',
            # not by naming a path
          - key: FREQ_MAX
            description: >-
              To (when OPT_USE true)
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'FREQ_RANGE' by numbering it '(3)',
            # not by naming a path
      - key: STURM_SEQ
        description: >-
          Sturm Sequence Check
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
  - when:
      - path: ANAL_TYPE
        equals: RITZ
    source:
      table: "Parameters — Ritz Vectors (`ANAL_TYPE = \"RITZ\"`)"
      line: 607
    fields:
      - key: GLINK_VECTOR
        description: >-
          Include GL-link Force Vectors
        type: object
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: OPT_USE
            description: >-
              Use Option
            type: boolean
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'GLINK_VECTOR' by numbering it '(1)',
            # not by naming a path
          - key: GLINK_NUMBER
            description: >-
              Number of Generations (when OPT_USE true)
            type: integer
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'GLINK_VECTOR' by numbering it '(2)',
            # not by naming a path
      - key: RITZ_LOAD
        description: >-
          Ritz Load Cases
        type: array
        items:
          type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: TYPE
            description: >-
              Type (Ground Acc.: "GROUND" / Load: "LOAD")
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            enum: [GROUND, LOAD]
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'RITZ_LOAD' by numbering it '(1)', not
            # by naming a path
          - key: LOAD_NAME
            description: >-
              Load Name ("ACCX"/"ACCY"/"ACCZ" 또는 하중케이스명)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'RITZ_LOAD' by numbering it '(2)', not
            # by naming a path
          - key: NUM_OF_GEN
            description: >-
              Number of Generations
            type: integer
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'RITZ_LOAD' by numbering it '(3)', not
            # by naming a path

sdkRules:
  - id: db-eigv-m1-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-eigv-m1-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 590
  table: Parameters — 공통
