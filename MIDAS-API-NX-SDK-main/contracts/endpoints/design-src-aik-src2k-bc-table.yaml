# /DESIGN/SRC/AIK-SRC2K/BC-TABLE
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-src-aik-src2k-bc-table
endpoint: /DESIGN/SRC/AIK-SRC2K/BC-TABLE
name: SRC Beam Checking Table

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 27_Design_SRC_AIKSRC2K.md
    section: "16. `DESIGN/SRC/AIK-SRC2K/BC-TABLE` — SRC Beam Checking Table (SRC 보 검토 테이블)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: getSrcBeamCheckTable
      modulePath: [design, srcAiksrc2k]
      argumentTypeName: SrcMemberCheckTableArgument
      documentation: |-
        docs/manual/27_Design_SRC_AIKSRC2K.md #16 — BC-TABLE — SRC Beam Check
        Table. COMPONENTS accepts: "MEMB"/"SECT"/"Span"/"Section"/"Bc"/"Hc"/
        "Material"/"Fy"/"fc"/"Fyr"/"Fys"/"POS"/"CHK"/"AsTop"/"AsBot"/"N_M"/
        "LCB_N"/"N_Mrs"/"Rat_N"/"P_M"/"LCB_P"/"P_Mrs"/"Rat_P"/"V"/"LCB_V"/"Vrs"/
        "Rat_V" (27 total). Response: ``{table_name_or_"Result Table": {"FORCE":
        ..., "DIST": ..., "HEAD": [...27 columns...], "DATA": [[...], ...]}}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: TABLE_TYPE
    description: >-
      결과표 타입 — 가능값: MEMB, PROP
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 번호 입력.
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          개별 요소 ID 지정
        type: array
        items:
          type: integer
        minItems: 1
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
      - key: TO
        description: >-
          요소 ID 범위 지정 (예: "1to160")
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹 이름 지정
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
  - key: SECTIONS
    description: >-
      표에 포함할 단면 번호 목록.
    type: array
    items:
      type: integer
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: PRI_SORT
    description: >-
      부재 기준 출력의 정렬 기준 (단면번호 또는 부재번호) — 0=SECT; 1=MEMB
    type: integer
    requirement: optional
    documentedDefault: 1
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: RESULT
    description: >-
      검토 상태로 결과 필터링 — 0=전체; 1=OK; 2=NG
    type: integer
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: TABLE_NAME
    description: >-
      결과표 제목
    type: string
    requirement: optional
    documentedDefault: SRC Checking Result
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      결과표 저장 경로
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: UNIT
    description: >-
      결과 단위 설정
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: FORCE
        description: >-
          힘 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' with a tree marker rather than
        # a path
      - key: DIST
        description: >-
          길이/거리 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' with a tree marker rather than
        # a path
      - key: HEAT
        description: >-
          열 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' with a tree marker rather than
        # a path
      - key: TEMP
        description: >-
          온도 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' with a tree marker rather than
        # a path
  - key: STYLES
    description: >-
      결과 숫자 형식
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: FORMAT
        description: >-
          숫자 형식 — 가능값: Default, Fixed, Scientific, General
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'STYLES' with a tree marker rather
        # than a path
      - key: PLACE
        description: >-
          소수 자릿수
        type: integer
        minimum: 0
        maximum: 15
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'STYLES' with a tree marker rather
        # than a path
  - key: COMPONENTS
    description: >-
      SRC 검토 결과표 구성 항목
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 27_Design_SRC_AIKSRC2K.md line 2756
  table: 파라미터
