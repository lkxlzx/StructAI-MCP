# /db/HSTG
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-hstg
endpoint: /db/HSTG
name: Define Construction Stage for Hydration
surface:
  className: ConstructionStageForHydration
  exportName: constructionStageForHydration
  modulePath: [db, constructionStage]
  payloadTypeName: ConstructionStageForHydrationPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 10_DB_Construction_Stage.md
    section: 14. /db/HSTG – Define Construction Stage for Hydration
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-17
    - product: gen
      ref: db-write-sweep-2026-08-17

operations:
  - method: DELETE
    variant: per_id
    path: /db/HSTG/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      수화열 단계명
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: bINITAL_TEMP
    description: >-
      초기 온도 사용 여부
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: INITIAL_TEMP
    description: >-
      초기 온도
    type: number
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ADD_STEP
    description: >-
      추가 스텝 목록
    type: array
    items:
      type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ACT_ELEM
    description: >-
      활성화 구조 그룹 목록
    type: array
    items:
      type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ACT_BNGR
    description: >-
      활성화 경계 그룹 목록
    type: array
    items:
      type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DACT_BNGR
    description: >-
      비활성화 경계 그룹 목록
    type: array
    items:
      type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ACT_LOAD
    description: >-
      활성화 하중 그룹 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: LOAD_NAME
        description: >-
          하중 케이스명
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ACT_LOAD' by numbering it '(1)', not
        # by naming a path
      - key: DAY
        description: >-
          활성화 일
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ACT_LOAD' by numbering it '(2)', not
        # by naming a path
  - key: DACT_LOAD
    description: >-
      비활성화 하중 그룹 목록
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: []
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: LOAD_NAME
        description: >-
          하중 케이스명
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'DACT_LOAD' by numbering it '(1)',
        # not by naming a path
      - key: DAY
        description: >-
          비활성화 일
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'DACT_LOAD' by numbering it '(2)',
        # not by naming a path

sdkRules:
  - id: db-hstg-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-hstg-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 10_DB_Construction_Stage.md line 1431
  table: 14-2. 파라미터
