# /db/THFC
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-thfc
endpoint: /db/THFC
name: Time History Functions
surface:
  className: TimeHistoryFunction
  exportName: timeHistoryFunction
  modulePath: [db, dynamicLoads]
  payloadTypeName: TimeHistoryFunctionPayload
  nestedTypes:
    - {path: aFUNCDATA, name: TimeValuePoint, namespace: DbBaseTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 09_DB_Dynamic_Loads.md
    section: 8. /db/THFC – Time History Functions
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-16
    - product: gen
      ref: db-write-sweep-2026-08-16

operations:
  - method: DELETE
    variant: per_id
    path: /db/THFC/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      함수명
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DESC
    description: >-
      설명
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: iTYPE
    description: >-
      데이터 타입 (1=정규화가속도, 2=가속도, 3=힘, 4=모멘트, 5=Normal)
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: GRAV
    description: >-
      중력 가속도
    type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: FUNCTYPE
    description: >-
      함수 타입 (1=Time Function, 2=Sinusoidal)
    type: integer
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: iMETHOD
    description: >-
      스케일 방법 (0=Scale Factor, 1=Max Value)
    type: integer
    requirement: required
    condition: Time Function (FUNCTYPE=1) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 1
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SCALE
    description: >-
      스케일 계수 (iMETHOD=0 시)
    type: number
    requirement: required
    condition: Time Function (FUNCTYPE=1) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 1
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: MAXVALUE
    description: >-
      최대 값 (iMETHOD=1 시)
    type: number
    requirement: optional
    condition: Time Function (FUNCTYPE=1) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 1
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's TimeHistoryFunction case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: aFUNCDATA
    description: >-
      시간-값 데이터 목록
    type: array
    items:
      type: object
    requirement: required
    condition: Time Function (FUNCTYPE=1) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 1
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: TIME
        description: >-
          시간
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'aFUNCDATA' by numbering it '(1)', not
        # by naming a path
      - key: VALUE
        description: >-
          값
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'aFUNCDATA' by numbering it '(2)', not
        # by naming a path
  - key: CONS_A
    description: >-
      상수 A
    type: number
    requirement: required
    condition: Sinusoidal (FUNCTYPE=2) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 2
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's TimeHistoryFunction case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: CONS_C
    description: >-
      상수 C
    type: number
    requirement: required
    condition: Sinusoidal (FUNCTYPE=2) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 2
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's TimeHistoryFunction case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: FREQUENCY
    description: >-
      주파수
    type: number
    requirement: required
    condition: Sinusoidal (FUNCTYPE=2) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 2
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's TimeHistoryFunction case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: DAMP_FACTOR
    description: >-
      감쇠 계수
    type: number
    requirement: required
    condition: Sinusoidal (FUNCTYPE=2) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 2
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's TimeHistoryFunction case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: PHASE_ANGLE
    description: >-
      위상각
    type: number
    requirement: required
    condition: Sinusoidal (FUNCTYPE=2) 추가 파라미터
    appliesWhen:
      - path: FUNCTYPE
        equals: 2
    documentedDefault: null
    documentedOptional: false
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's TimeHistoryFunction case completed a live
      create-read-update-delete round trip on gen and civil without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual

sdkRules:
  - id: db-thfc-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-thfc-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 09_DB_Dynamic_Loads.md line 1972
  table: 8-2. 파라미터
