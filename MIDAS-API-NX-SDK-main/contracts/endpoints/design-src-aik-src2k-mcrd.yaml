# /DESIGN/SRC/AIK-SRC2K/MCRD
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-src-aik-src2k-mcrd
endpoint: /DESIGN/SRC/AIK-SRC2K/MCRD
name: Modify SRC Column Section Data
surface:
  className: SrcColumnSectionData
  exportName: srcColumnSectionData
  modulePath: [design, srcAiksrc2k]
  payloadTypeName: SrcColumnSectionDataPayload
  nestedTypes:
    - {path: MAIN_BAR, name: SrcColumnSectionMainBar, namespace: DesignSrcAiksrc2kTypes}
    - {path: SHEAR_BAR, name: SrcColumnSectionShearBar, namespace: DesignSrcAiksrc2kTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 27_Design_SRC_AIKSRC2K.md
    section: "25. `DESIGN/SRC/AIK-SRC2K/MCRD` — Modify SRC Column Section Data (SRC 기둥 단면 데이터 수정)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: MAIN_BAR
    description: >-
      주철근 데이터.
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: USE_REBAR_SPACE
        description: >-
          자동 계산된 철근 간격.
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'MAIN_BAR' with a tree marker rather
        # than a path
      - key: REBAR_SPACE
        description: >-
          주철근 간격. USE_REBAR_SPACE가 false일 때 사용.
        type: number
        minimum: 0
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'MAIN_BAR' with a tree marker rather
        # than a path
      - key: NUM
        description: >-
          주철근 총 개수. 4의 배수여야 함.
        type: integer
        minimum: 4
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'MAIN_BAR' with a tree marker rather
        # than a path
      - key: NAME
        description: >-
          주철근 규격. — D4=D4; D5=D5; D6=D6; D7=D7; D8=D8; D10=D10; D13=D13; D16=D16;
          D19=D19; D22=D22; D25=D25; D29=D29 …(전체 19개)
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'MAIN_BAR' with a tree marker rather
        # than a path
      - key: ROW
        description: >-
          사각형 단면의 철근 열 수. 2의 배수여야 함.
        type: integer
        minimum: 2
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'MAIN_BAR' with a tree marker rather
        # than a path
      - key: DO
        description: >-
          콘크리트 피복 / 중심간 거리 d0.
        type: number
        minimum: 0
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'MAIN_BAR' with a tree marker rather
        # than a path
  - key: SHEAR_BAR
    description: >-
      후프/타이 철근 데이터.
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: NAME
        description: >-
          후프/타이 철근 규격. — D4=D4; D5=D5; D6=D6; D7=D7; D8=D8; D10=D10; D13=D13;
          D16=D16; D19=D19; D22=D22; D25=D25; D29=D29 …(전체 19개)
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'SHEAR_BAR' with a tree marker
        # rather than a path
      - key: DIST
        description: >-
          후프/타이 철근 간격. USE_REBAR_SPACE가 false일 때 사용.
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'SHEAR_BAR' with a tree marker rather
        # than a path

extraction:
  source: 27_Design_SRC_AIKSRC2K.md line 5863
  table: 파라미터
