# /DESIGN/RC/KDS-41-20-2022/HCD-TABLE
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-hcd-table
endpoint: /DESIGN/RC/KDS-41-20-2022/HCD-TABLE
name: RC Haunched Beam Design Table

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "52. `DESIGN/RC/KDS-41-20-2022/HCD-TABLE` — RC Haunched Beam Design Table (RC 헌치보 설계 테이블)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: getHaunchedBeamDesignTable
      modulePath: [design, rcKds, designForces]
      argumentTypeName: RcHaunchedBeamDesignTableArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #52 — HCD-TABLE — RC Haunched
        Beam Design Table. Multiple rows per element (one per haunch
        section/POS). Response: ``{table_name_or_"Result Table": {"FORCE": ...,
        "DIST": ..., "HEAD": [...20 columns...], "DATA": [[...], ...]}}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      헌치보 요소 지정 — KEYS/TO/STRUCTURE_GROUP_NAME 중 하나
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: RESULT
    description: >-
      결과 필터 (0=All, 1=OK, 2=NG)
    type: integer
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: TABLE_NAME
    description: >-
      표 제목 · 저장 경로 · 단위 · 포맷 · 표시 열
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      표 제목 · 저장 경로 · 단위 · 포맷 · 표시 열
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: UNIT
    description: >-
      표 제목 · 저장 경로 · 단위 · 포맷 · 표시 열
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: STYLES
    description: >-
      표 제목 · 저장 경로 · 단위 · 포맷 · 표시 열
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: COMPONENTS
    description: >-
      표 제목 · 저장 경로 · 단위 · 포맷 · 표시 열
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 9040
  table: 파라미터
