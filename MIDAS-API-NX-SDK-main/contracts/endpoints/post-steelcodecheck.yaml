# /post/STEELCODECHECK
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: post-steelcodecheck
endpoint: /post/STEELCODECHECK
name: Steel Code Check

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 23_POST_Design.md
    section: 2. Steel Code Check
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-31
    - product: gen
      ref: db-read-sweep-2026-08-13

operations:
  - method: POST
    surface:
      exportName: getSteelCodeCheck
      modulePath: [post, design]
      takesArgument: false
      documentation: |-
        docs/manual/23_POST_Design.md #2 — /post/STEELCODECHECK — Steel Code Check.

        Takes no arguments; returns ``{"vSECT": [...], "vELEM": [...]}`` — each
        entry has SECT/ELEM id, RAT (combined strength ratio), SLN (slenderness
        ratio), DEF (deflection), DEFA (allowable deflection).
    risk: read_only
    request:
      wrapper: argument
      itemSchema: empty
    response:
      wrapper: raw
    notes: >-
      A POST-shaped read: it returns design results and changes nothing. The
      manual's request example is `{"Argument": {}}`. Its only table is
      titled "Response 필드", and the fields below transcribe that table: they
      describe the body the server returns, `{"vSECT": [...], "vELEM": [...]}`
      with no wrapper, not anything a caller sends. Live, on both products,
      an analyzed model with no steel code check run answered
      `{"message": ""}` instead; that is the nothing-to-show shape, not a
      failure. A populated response has never been observed.

fields:
  - key: vSECT
    description: >-
      단면 데이터 배열
    type: array
    items:
      type: object
    requirement: unstated
    documentedDefault: null
    documentedOptional: null
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: SECT
        description: >-
          단면 ID
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vSECT' by numbering it '1-(1)',
        # not by naming a path
      - key: RAT
        description: >-
          조합 강도비 (Combined Strength Ratio)
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vSECT' by numbering it '1-(2)',
        # not by naming a path
      - key: SLN
        description: >-
          세장비 (Slenderness Ratio)
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vSECT' by numbering it '1-(3)',
        # not by naming a path
      - key: DEF
        description: >-
          처짐 (Deflection)
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vSECT' by numbering it '1-(4)',
        # not by naming a path
      - key: DEFA
        description: >-
          허용 처짐 (Allowable Deflection)
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vSECT' by numbering it '1-(5)',
        # not by naming a path
  - key: vELEM
    description: >-
      요소 데이터 배열
    type: array
    items:
      type: object
    requirement: unstated
    documentedDefault: null
    documentedOptional: null
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: ELEM
        description: >-
          요소 ID
        type: integer
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vELEM' by numbering it '2-(1)',
        # not by naming a path
      - key: RAT
        description: >-
          조합 강도비
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vELEM' by numbering it '2-(2)',
        # not by naming a path
      - key: SLN
        description: >-
          세장비
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vELEM' by numbering it '2-(3)',
        # not by naming a path
      - key: DEF
        description: >-
          처짐
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vELEM' by numbering it '2-(4)',
        # not by naming a path
      - key: DEFA
        description: >-
          허용 처짐
        type: number
        requirement: unstated
        documentedDefault: null
        documentedOptional: null
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vELEM' by numbering it '2-(5)',
        # not by naming a path

extraction:
  source: 23_POST_Design.md line 167
  table: Response 필드
  missingColumns:
    - heading: Response 필드
      line: 167
      columns: [Default]
