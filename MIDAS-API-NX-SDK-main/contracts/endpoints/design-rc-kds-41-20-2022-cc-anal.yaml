# /DESIGN/RC/KDS-41-20-2022/CC-ANAL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-cc-anal
endpoint: /DESIGN/RC/KDS-41-20-2022/CC-ANAL
name: RC Column Check Perform

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "57. `DESIGN/RC/KDS-41-20-2022/CC-ANAL` — RC Column Check Perform (RC 기둥 검토 수행)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-30
    - product: gen
      ref: db-read-sweep-2026-07-30

operations:
  - method: POST
    surface:
      exportName: performColumnCheck
      modulePath: [design, rcKds, checks]
      argumentTypeName: PerformRcMemberCheckArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #57 — CC-ANAL — RC Column
        Check Perform. Runs P-M interaction/shear code-checking on RC column
        members that already have rebar assigned. Response: ``{"message":
        "success"}``.

        ✅ **Re-verified 2026-07-25: ran clean 4/4 — on the same build that hung.**
        An independent production tool (the QuickRebar NX web app) ran this
        call — including ``PERFORM_TYPE: "ALL"`` — plus ``perform_beam_check``
        cleanly 4/4 with the app staying connected. This lifts the blanket
        "never call this" rule — but see the caveat below before treating it as
        fixed.

        ⚠️ CONFIRMED to stall on **Gen NX 2026 (v2.1), build 06/23/2026**: the
        desktop app's progress dialog got stuck at "Converting Design Results... 0%",
        5 times out of 5 whenever the target had real rebar data, a recognized
        design load combination, and confirmed analysis results — on natively
        KDS-configured production models, not just synthetic setups, and
        independent of admin/elevated privileges. On one occasion the app's own
        message log showed the check had actually finished while the dialog
        stayed frozen at 0%, suggesting a stuck completion-signal bug rather
        than the check itself deadlocking. Consequences varied: a forced process
        kill 3 of 5 times, clean recovery via "Stop Execution" the other 2.
        Every call that didn't hit the precondition combination instead returned
        a clean, correctly-shaped ``{"error": ...}`` response, so the request
        shape itself was never the problem.

        ⚠️ **The clean re-verification ran on that exact same build** — this was
        not fixed by a vendor patch, so the real trigger is still unidentified
        (model state, call sequence, and client/transport are all untested
        candidates). Every test on both sides also used the **KDS 41 20:2022**
        code specifically. Treat the defensive pattern as the actual mitigation:
        use a short timeout, and read ``get_column_check_table`` regardless of
        whether this call returned — a timed-out check very likely still
        committed its results, which is exactly what the original reproductions
        showed (full OK/NG, P-M ratios, and assigned rebar came back immediately
        after a "hung" call, with no retry needed). See
        docs/live_verification_notes.md for the full history.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PERFORM_TYPE
    description: >-
      수행 대상 타입 ("ALL"=전체, "ELEMS"=요소별, "SECTIONS"=단면별)
    type: string
    requirement: optional
    documentedDefault: ALL
    documentedOptional: true
    enum: [ALL, ELEMS, SECTIONS]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 입력 (ELEMS / SECTIONS 중 하나)
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          개별 ID
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.1', not by
        # naming a path
      - key: TO
        description: >-
          ID 범위 (예 "1to160")
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.2', not by
        # naming a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹 이름
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' by numbering it '3.3', not by
        # naming a path
  - key: SECTIONS
    description: >-
      단면 번호 (ELEMS / SECTIONS 중 하나)
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 10133
  table: 파라미터
