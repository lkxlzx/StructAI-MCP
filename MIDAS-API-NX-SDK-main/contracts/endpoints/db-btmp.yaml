# /db/BTMP
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-btmp
endpoint: /db/BTMP
name: Beam Section Temperature
surface:
  className: BeamSectionTemperature
  exportName: beamSectionTemperature
  modulePath: [db, temperaturePrestress]
  payloadTypeName: BeamSectionTemperaturePayload
  nestedTypes:
    - {path: ITEMS, name: BeamSectionTemperatureItem, namespace: DbTemperaturePrestressTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 07_DB_Temperature_Prestress.md
    section: 3. /db/BTMP — Beam Section Temperature
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-16
    - product: gen
      ref: db-write-sweep-2026-08-16

operations:
  - method: DELETE
    variant: per_id
    path: /db/BTMP/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: ITEMS
    description: >-
      Beam Section Temperature (배열 오브젝트로 입력)
    type: array
    items:
      type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: ID
        description: >-
          Serial Number
        type: integer
        requirement: optional
        documentedDefault: 0
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(1)', not
        # by naming a path
      - key: LCNAME
        description: >-
          Load Case Name
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(2)', not
        # by naming a path
      - key: GROUP_NAME
        description: >-
          Load Group Name
        type: string
        requirement: optional
        documentedDefault: ""
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(3)', not
        # by naming a path
      - key: DIR
        description: >-
          Direction · Local-y: "LY" · Local-z: "LZ"
        type: string
        requirement: optional
        documentedDefault: LZ
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(4)', not
        # by naming a path
      - key: REF
        description: >-
          Ref. Position · "Centroid" · "+End(Top)" → "Top" · "-End(Bot)" → "Bot"
        type: string
        requirement: optional
        documentedDefault: Centroid
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(5)', not
        # by naming a path
      - key: NUM
        description: >-
          Number of Section Temperature (vSECTTMP 항목 수)
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(6)', not
        # by naming a path
      - key: bPSC
        description: >-
          Section Type · General: false · PSC/Composite: true
        type: boolean
        requirement: optional
        documentedDefault: false
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(7)', not
        # by naming a path
      - key: vSECTTMP
        description: >-
          Section Temperature List
        type: array
        items:
          type: object
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ITEMS' by numbering it '(8)', not
        # by naming a path
        properties:
          - key: TYPE
            description: >-
              Material Type · Element: "ELEMENT" · Input: "INPUT"
            type: string
            requirement: optional
            documentedDefault: ELEMENT
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'i',
            # not by naming a path
          - key: VAL_B
            description: >-
              B Value
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'ii',
            # not by naming a path
          - key: VAL_H1
            description: >-
              H1 Value
            type: number
            requirement: optional
            documentedDefault: null
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'iii',
            # not by naming a path
          - key: VAL_H2
            description: >-
              H2 Value
            type: number
            requirement: optional
            documentedDefault: null
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'iv',
            # not by naming a path
          - key: VAL_T1
            description: >-
              T1 Value
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'v',
            # not by naming a path
          - key: VAL_T2
            description: >-
              T2 Value
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'vi',
            # not by naming a path
          - key: ELAST
            description: >-
              Modulus of Elasticity (TYPE = "INPUT" 일 때)
            type: number
            requirement: optional
            documentedDefault: null
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'vii',
            # not by naming a path
          - key: THERMAL
            description: >-
              Thermal Coefficient (TYPE = "INPUT" 일 때)
            type: number
            requirement: optional
            documentedDefault: null
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'viii',
            # not by naming a path
          - key: REF
            description: >-
              Ref. (bPSC = true) · Top: 0 · Bottom: 1
            type: integer
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'ix',
            # not by naming a path
          - key: OPT_B
            description: >-
              B-Type (bPSC = true) · Section: 0 · Value: 1
            type: integer
            requirement: optional
            documentedDefault: 1
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'x',
            # not by naming a path
          - key: OPT_H1
            description: >-
              H1-Type (bPSC = true) · Z1:0 · Z2:1 · Z3:2 · Value:3
            type: integer
            requirement: optional
            documentedDefault: 3
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'xi',
            # not by naming a path
          - key: OPT_H2
            description: >-
              H2-Type (bPSC = true) · Z1:0 · Z2:1 · Z3:2 · Value:3
            type: integer
            requirement: optional
            documentedDefault: 3
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual nests this under 'vSECTTMP' by numbering it 'xii',
            # not by naming a path

sdkRules:
  - id: db-btmp-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-btmp-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 07_DB_Temperature_Prestress.md line 379
  table: 파라미터
