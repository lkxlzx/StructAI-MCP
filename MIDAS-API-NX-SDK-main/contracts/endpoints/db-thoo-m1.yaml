# /db/THOO-M1
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-thoo-m1
endpoint: /db/THOO-M1
name: Time History Output Option (Hyper-S)
surface:
  className: TimeHistoryOutputOptionHyperS
  exportName: timeHistoryOutputOptionHyperS
  modulePath: [db, dynamicLoads]
  payloadTypeName: TimeHistoryOutputOptionHyperSPayload
  nestedTypes:
    - {path: OUT_OPT, name: HyperSOutputOption, namespace: DbDynamicLoadsTypes}
    - {path: RESULT_SELECTION, name: HyperSResultSelection, namespace: DbDynamicLoadsTypes}

products: [civil]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 09_DB_Dynamic_Loads.md
    section: 5. /db/THOO-M1 – Time History Output Option (Hyper-S)
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_civil
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    variant: per_id
    path: /db/THOO-M1/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: OUT_OPT
    description: >-
      비선형 해석 결과 출력 옵션
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: HINGE_OUT
        description: >-
          비탄성 힌지 스텝별 출력 옵션 (0=전체 요소, 1=선택 요소, 2=미출력)
        type: integer
        requirement: required
        documentedDefault: null
        documentedOptional: false
        enum: [0, 1, 2]
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'OUT_OPT' by numbering it '(1)', not
        # by naming a path
      - key: COMMON_OPT
        description: >-
          공통 설정 여부 (true=FIBER_OUT이 HINGE_OUT과 동일)
        type: boolean
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'OUT_OPT' by numbering it '(2)', not
        # by naming a path
      - key: FIBER_OUT
        description: >-
          섬유 단면 스텝별 출력 옵션 (0=전체 요소, 1=선택 요소, 2=미출력)
        type: integer
        requirement: conditional
        condition: COMMON_OPT=false 시
        documentedDefault: null
        documentedOptional: false
        enum: [0, 1, 2]
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'OUT_OPT' by numbering it '(3)', not
        # by naming a path
  - key: RESULT_SELECTION
    description: >-
      시간이력 결과 옵션
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: ENERGY_RESULT
        description: >-
          에너지 결과 출력
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'RESULT_SELECTION' by numbering it
        # '(1)', not by naming a path
      - key: SDVI
        description: >-
          점성 감쇠기 / 오일 감쇠기 결과
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'RESULT_SELECTION' by numbering it
        # '(2)', not by naming a path
      - key: SDVE
        description: >-
          점탄성 감쇠기 결과
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'RESULT_SELECTION' by numbering it
        # '(3)', not by naming a path
      - key: SDST
        description: >-
          강재 감쇠기 결과
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'RESULT_SELECTION' by numbering it
        # '(4)', not by naming a path
      - key: SDHY
        description: >-
          이력 절연 장치 결과
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'RESULT_SELECTION' by numbering it
        # '(5)', not by naming a path
      - key: SDIS
        description: >-
          절연 장치 결과
        type: boolean
        requirement: optional
        documentedDefault: true
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'RESULT_SELECTION' by numbering it
        # '(6)', not by naming a path

sdkRules:
  - id: db-thoo-m1-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-thoo-m1-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 09_DB_Dynamic_Loads.md line 1218
  table: 5-2. 파라미터
