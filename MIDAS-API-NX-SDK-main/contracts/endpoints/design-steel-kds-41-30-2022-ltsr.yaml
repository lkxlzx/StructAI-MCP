# /DESIGN/STEEL/KDS-41-30-2022/LTSR
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-ltsr
endpoint: /DESIGN/STEEL/KDS-41-30-2022/LTSR
name: Limiting Slenderness Ratio
surface:
  className: LimitingSlendernessRatio
  exportName: limitingSlendernessRatio
  modulePath: [design, steelKds]
  payloadTypeName: LimitingSlendernessRatioPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "14. `DESIGN/STEEL/KDS-41-30-2022/LTSR` — Limiting Slenderness Ratio (세장비 제한)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: bNOTCHECK
    description: >-
      세장비 검토 안함
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: COMP
    description: >-
      압축 세장비 제한
    type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: TENS
    description: >-
      인장 세장비 제한
    type: number
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 2565
  table: 파라미터
