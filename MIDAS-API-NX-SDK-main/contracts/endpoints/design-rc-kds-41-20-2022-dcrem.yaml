# /DESIGN/RC/KDS-41-20-2022/DCREM
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-dcrem
endpoint: /DESIGN/RC/KDS-41-20-2022/DCREM
name: Same Beam Rebar at Joints
surface:
  className: EqualizeJointBeamRebar
  exportName: equalizeJointBeamRebar
  modulePath: [design, rcKds, rebar]
  payloadTypeName: EqualizeJointBeamRebarPayload
  nestedTypes:
    - {path: SELECTED_MEMBERS, name: EqualizeJointBeamRebarSelectedMember, namespace: DesignRcKdsRebarTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "34. `DESIGN/RC/KDS-41-20-2022/DCREM` — Same Beam Rebar at Joints (접합부 보 철근 동일화)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      인덱스 문자열을 키로 갖는 맵
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SELECT_ALL
    description: >-
      모든 부재에 적용
    type: boolean
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SELECTED_MEMBERS
    description: >-
      선택 부재 맵 (SELECT_ALL=false 일 때 필수). 키는 절점 ID 문자열
    type: object
    requirement: conditional
    condition: SELECT_ALL=false
    appliesWhen:
      - path: SELECT_ALL
        equals: false
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: ELEM_LIST
        description: >-
          절점을 사이에 두는 정확히 2개의 요소 번호
        type: array
        items:
          type: integer
        minItems: 2
        maxItems: 2
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'SELECTED_MEMBERS' by numbering it
        # '3.1', not by naming a path

extraction:
  source: 26_Design_RC_KDS41202022.md line 6158
  table: 파라미터
