# /DESIGN/SRC/AIK-SRC2K/CC-REPORT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-src-aik-src2k-cc-report
endpoint: /DESIGN/SRC/AIK-SRC2K/CC-REPORT
name: SRC Column Checking Report

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 27_Design_SRC_AIKSRC2K.md
    section: "20. `DESIGN/SRC/AIK-SRC2K/CC-REPORT` — SRC Column Checking Report (SRC 기둥 검토 리포트)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: exportSrcColumnCheckReport
      modulePath: [design, srcAiksrc2k]
      argumentTypeName: SrcColumnCheckReportArgument
      documentation: |-
        docs/manual/27_Design_SRC_AIKSRC2K.md #20 — CC-REPORT — SRC Column
        Check Report. Response: ``{"SUCCESS": bool, "FILE_PATH": str, "MESSAGE":
        str}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: REPORT_TYPE
    description: >-
      보고서 표 타입 — 가능값: MEMB, PROP
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [MEMB, PROP]
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE_MEMB
    description: >-
      요소 기준 보고서의 출력 모드 — Graphic=그래픽 (JPG 이미지); Detail=상세 (DOC 문서); Summary=요약
      (TXT 텍스트)
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: CURRENT_MODE_PROP
    description: >-
      특성 기준 보고서의 출력 모드 — Graphic=그래픽 (JPG 이미지); Summary=요약 (TXT 텍스트)
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 번호 입력.
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          개별 ID 지정
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
      - key: TO
        description: >-
          ID 범위 지정 (예: '1to160')
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹 이름 지정
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'ELEMS' with a tree marker rather than
        # a path
  - key: SECTIONS
    description: >-
      보고서에 포함할 단면 번호 목록.
    type: array
    items:
      type: integer
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      보고서 파일을 저장할 디렉토리 경로
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: OUTPUT_NAME
    description: >-
      출력 파일 기본 이름. 요소가 여러 개인 경우 파일명 앞에 순번과 요소번호가 붙습니다(예: 001_E100_filename.jpg,
      002_E865_filename.jpg).
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 27_Design_SRC_AIKSRC2K.md line 3949
  table: 파라미터
