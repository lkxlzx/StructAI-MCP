# /DESIGN/RC/KDS-41-20-2022/HCD-ANAL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-hcd-anal
endpoint: /DESIGN/RC/KDS-41-20-2022/HCD-ANAL
name: RC Haunched Beam Design Perform

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "51. `DESIGN/RC/KDS-41-20-2022/HCD-ANAL` — RC Haunched Beam Design Perform (RC 헌치보 설계 수행)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: performHaunchedBeamDesign
      modulePath: [design, rcKds, designForces]
      argumentTypeName: RcHaunchedBeamDesignPerformArgument
      documentation: |-
        docs/manual/26_Design_RC_KDS41202022.md #51 — HCD-ANAL — RC Haunched
        Beam Design Perform. Response: ``{"message": "success"}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      헌치보 요소 지정 — KEYS/TO/STRUCTURE_GROUP_NAME 중 하나
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: KEYS
        description: >-
          요소 ID 각각 지정
        type: array
        items:
          type: integer
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: TO
        description: >-
          요소 ID 범위 (예 "1to160")
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
      - key: STRUCTURE_GROUP_NAME
        description: >-
          구조 그룹명
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 8943
  table: 파라미터
