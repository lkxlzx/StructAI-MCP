# /DESIGN/RC/KDS-41-20-2022/DCTL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-dctl
endpoint: /DESIGN/RC/KDS-41-20-2022/DCTL
name: Definition of Frame
surface:
  className: DefinitionOfFrame
  exportName: definitionOfFrame
  modulePath: [design, rcKds, setup]
  payloadTypeName: DefinitionOfFramePayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "2. `DESIGN/RC/KDS-41-20-2022/DCTL` — Definition of Frame (프레임 정의)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼 (ID 문자열 키, 1개)
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: FRAMEX
    description: >-
      X방향 프레임 (Unbraced Sway=비횡지지/횡변위, Braced Non-sway=횡지지/무횡변위)
    type: string
    requirement: optional
    documentedDefault: Braced Non-sway
    documentedOptional: true
    enum: [Sway, Non-sway]
    safeToOmit: unverified
    provenance: manual
  - key: FRAMEY
    description: >-
      Y방향 프레임 (Unbraced Sway=비횡지지/횡변위, Braced Non-sway=횡지지/무횡변위)
    type: string
    requirement: optional
    documentedDefault: Braced Non-sway
    documentedOptional: true
    enum: [Sway, Non-sway]
    safeToOmit: unverified
    provenance: manual
  - key: bAUTOKF
    description: >-
      유효좌굴길이계수 자동계산
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: DT
    description: >-
      설계 타입 (3D / XZ / YZ / XY 평면)
    type: string
    requirement: optional
    documentedDefault: 3D
    documentedOptional: true
    enum: [3D, XZ, YZ, XY]
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 834
  table: 파라미터
