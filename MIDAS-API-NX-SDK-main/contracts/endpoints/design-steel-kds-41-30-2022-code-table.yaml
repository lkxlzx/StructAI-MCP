# /DESIGN/STEEL/KDS-41-30-2022/CODE-TABLE
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-code-table
endpoint: /DESIGN/STEEL/KDS-41-30-2022/CODE-TABLE
name: Steel Code Check Table

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "24. `DESIGN/STEEL/KDS-41-30-2022/CODE-TABLE` — Steel Code Check Table (강재 코드 검토 표)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-10
    - product: gen
      ref: db-read-sweep-2026-08-10

operations:
  - method: POST
    surface:
      exportName: getSteelCodeCheckTable
      modulePath: [design, steelKds]
      argumentTypeName: SteelCodeCheckTableArgument
      documentation: |-
        docs/manual/25_Design_Steel_KDS41302022.md #24 — CODE-TABLE — Steel Code
        Check Table. Response: ``{table_name_or_"Result Table": {"FORCE": ...,
        "DIST": ..., "HEAD": [...], "DATA": [[...], ...]}}``.
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Argument
    description: >-
      Argument 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: TABLE_TYPE
    description: >-
      표 타입 ("MEMB"=부재기준, "PROP"=단면기준)
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [MEMB, PROP]
    safeToOmit: unverified
    provenance: manual
  - key: ELEMS
    description: >-
      요소 입력 (ELEMS/SECTIONS 중 하나) — KEYS/TO/STRUCTURE_GROUP_NAME
    type: object
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SECTIONS
    description: >-
      단면 번호 (ELEMS/SECTIONS 중 하나)
    type: array
    items:
      type: integer
    requirement: conditional
    condition: "oneOf: exactly one of \"ELEMS\", \"SECTIONS\" is required"
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: PRI_SORT
    description: >-
      1차 정렬 (0=SECT, 1=MEMB)
    type: integer
    requirement: optional
    documentedDefault: 1
    documentedOptional: true
    enum: [0, 1]
    safeToOmit: unverified
    provenance: manual
  - key: RESULT
    description: >-
      결과 필터 (0=All, 1=OK, 2=NG)
    type: integer
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    enum: [0, 1, 2]
    safeToOmit: unverified
    provenance: manual
  - key: VIEW_RATPC
    description: >-
      RatPc > 0.4 부재만 표시
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: TABLE_NAME
    description: >-
      응답 표 제목
    type: string
    requirement: optional
    documentedDefault: ""
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: EXPORT_PATH
    description: >-
      결과 파일 저장 경로
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: UNIT
    description: >-
      단위 설정
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: FORCE
        description: >-
          힘 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' by numbering it '10.1', not by
        # naming a path
      - key: DIST
        description: >-
          길이 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' by numbering it '10.2', not by
        # naming a path
      - key: HEAT
        description: >-
          열 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' by numbering it '10.3', not by
        # naming a path
      - key: TEMP
        description: >-
          온도 단위
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'UNIT' by numbering it '10.4', not by
        # naming a path
  - key: STYLES
    description: >-
      숫자 포맷
    type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: FORMAT
        description: >-
          포맷 (Default/Fixed/Scientific/General)
        type: string
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        enum: [Default, Fixed, Scientific, General]
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'STYLES' by numbering it '11.1', not
        # by naming a path
      - key: PLACE
        description: >-
          소수 자릿수 (0~15)
        type: integer
        minimum: 0
        maximum: 15
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'STYLES' by numbering it '11.2', not
        # by naming a path
  - key: COMPONENTS
    description: >-
      결과 표 성분 목록
    type: array
    items:
      type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 4752
  table: 파라미터
