# /DESIGN/STEEL/KDS-41-30-2022/SLRS
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-steel-kds-41-30-2022-slrs
endpoint: /DESIGN/STEEL/KDS-41-30-2022/SLRS
name: Seismic Load Resisting System by Member
surface:
  className: SeismicLoadResistingSystemByMember
  exportName: seismicLoadResistingSystemByMember
  modulePath: [design, steelKds]
  payloadTypeName: SeismicLoadResistingSystemByMemberPayload

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 25_Design_Steel_KDS41302022.md
    section: "19. `DESIGN/STEEL/KDS-41-30-2022/SLRS` — Seismic Load Resisting System by Member (부재별 내진 저항시스템)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      Assign 래퍼
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: FRAME_TYPE
    description: >-
      내진 저항시스템 골조 타입 ("Special Concentrically Braced Frames", "Ordinary
      Concentrically Braced Frames", "Eccentrically Braced Frames", "Buckling
      Restrained Braced Frames", "Special Plate Shear Walls")
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    enum: [Special Concentrically Braced Frames, Ordinary Concentrically Braced Frames, Eccentrically Braced Frames, Buckling Restrained Braced Frames, Special Plate Shear Walls]
    safeToOmit: unverified
    provenance: manual
  - key: CHECK_OPTION
    description: >-
      가새 세장비 검토 / 링크 검토 (Buckling Restrained·Special Plate Shear Walls는 미지원 →
      false 강제)
    type: boolean
    requirement: optional
    documentedDefault: true
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual

extraction:
  source: 25_Design_Steel_KDS41302022.md line 3315
  table: 파라미터
