# /DESIGN/SRC/AIK-SRC2K/LCTB
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-src-aik-src2k-lctb
endpoint: /DESIGN/SRC/AIK-SRC2K/LCTB
name: Load Contribution for Nonlinear Load Case
surface:
  className: SrcLoadContributionForNonlinearLoadCase
  exportName: srcLoadContributionForNonlinearLoadCase
  modulePath: [design, srcAiksrc2k]
  payloadTypeName: SrcLoadContributionForNonlinearLoadCasePayload
  nestedTypes:
    - {path: BASE_ITEM, name: SrcLoadContributionBaseItem, namespace: DesignSrcAiksrc2kTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 27_Design_SRC_AIKSRC2K.md
    section: "5. `DESIGN/SRC/AIK-SRC2K/LCTB` — Load Contribution for Nonlinear Load Case (비선형 하중케이스 하중기여)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: NAME
    description: >-
      하중기여 이름
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DESC
    description: >-
      설명
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: BASE_ITEM
    description: >-
      하중기여 항목
    type: array
    items:
      type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: FACTOR
        description: >-
          계수
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'BASE_ITEM' with a tree marker rather
        # than a path
      - key: LOAD_CASE_NAME
        description: >-
          하중케이스 이름
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'BASE_ITEM' with a tree marker rather
        # than a path

extraction:
  source: 27_Design_SRC_AIKSRC2K.md line 849
  table: 파라미터
