# /db/EPSE
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-epse
endpoint: /db/EPSE
name: Seismic Earth Pressure
surface:
  className: SeismicEarthPressure
  exportName: seismicEarthPressure
  modulePath: [db, staticLoads]
  payloadTypeName: SeismicEarthPressurePayload
  nestedTypes:
    - {path: PRES_PROFILE_ITEMS, name: SeismicEarthPressureProfileItem, namespace: DbStaticLoadsTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 06_DB_Static_Loads.md
    section: 18. /db/EPSE — Seismic Earth Pressure
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-08-16
    - product: gen
      ref: db-read-sweep-2026-08-16

operations:
  - method: DELETE
    variant: per_id
    path: /db/EPSE/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: LOADCASE
    description: >-
      Load Case Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: DIR
    description: >-
      Load Direction ("XY" / "NORMAL")
    type: string
    requirement: optional
    documentedDefault: XY
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ANGLE
    description: >-
      Seismic Earth Pressure Angle
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: IN_PT
    description: >-
      Inner Point
    type: number
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SF
    description: >-
      Scale Factor
    type: number
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: CODE
    description: >-
      Design Code
    type: string
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SEIS_LOAD
    description: >-
      Seismic Load Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: LAYER_PARAM
    description: >-
      Soil Layer Parameter ("SINGLE" / "DOUBLE")
    type: string
    requirement: optional
    documentedDefault: SINGLE
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: LAYER_LV
    description: >-
      Soil Second Layer Level (Double Cosine)
    type: number
    requirement: optional
    documentedDefault: 0
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: SOIL_PROP
    description: >-
      Soil Properties Name
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: SEL_TYPE
    description: >-
      Selection Type ("GROUP" / "ELEMENT")
    products: [gen]
    type: string
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
  - key: ELEM_TYPE
    description: >-
      Element Type ("FRAME" / "PLANAR")
    type: string
    requirement: optional
    documentedDefault: ELEM
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: NODE_LIST
    description: >-
      Node List
    type: array
    items:
      type: integer
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: ELEM_LIST
    description: >-
      Element List
    type: array
    items:
      type: integer
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: LOADING_AREA_GROUP
    description: >-
      Loading Area Group Name
    products: [gen]
    # NOTE: /info evidence, not the manual's. GET /info/db/EPSE declares this
    # on gen and not on the other product, captured 2026-09-03. The section
    # says nothing about either product, so this tag records a measurement
    # rather than a documentation claim. See MD-46.
    type: integer
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: PRES_PROFILE_ITEMS
    description: >-
      Pressure Profile items
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: LEVEL
        description: >-
          Level
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PRES_PROFILE_ITEMS' by numbering it
        # '(1)', not by naming a path
      - key: KH
        description: >-
          Horizontal Coefficient KH
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PRES_PROFILE_ITEMS' by numbering it
        # '(2)', not by naming a path
      - key: REL_DISP
        description: >-
          Relative Displacement
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PRES_PROFILE_ITEMS' by numbering it
        # '(3)', not by naming a path
      - key: SEIS_PRES
        description: >-
          Seismic Pressure
        type: number
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PRES_PROFILE_ITEMS' by numbering it
        # '(4)', not by naming a path
      - key: ADD_PRES
        description: >-
          Additional Pressure
        type: number
        requirement: optional
        documentedDefault: null
        documentedOptional: true
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'PRES_PROFILE_ITEMS' by numbering it
        # '(5)', not by naming a path
  - key: LOAD_TYPE
    description: >-
      Loading Type
    products: [civil]
    type: string
    requirement: unstated
    documentedDefault: null
    documentedOptional: null
    safeToOmit: unverified
    provenance: info_schema
    # RESOLVED: declared by /info and absent from the manual - the whole
    # manual was searched for the name, not just this section.
    # Civil NX only. The section describes one endpoint for both products and
    # says nothing about either; the `products` tag is a measurement from
    # /info, not a documentation claim. See MD-46.
  - key: WIDTH
    description: >-
      Width
    products: [civil]
    type: number
    requirement: unstated
    documentedDefault: null
    documentedOptional: null
    safeToOmit: unverified
    provenance: info_schema
    # RESOLVED: declared by /info and absent from the manual - the whole
    # manual was searched for the name, not just this section.

manualDefects:
  - describes: field_name
    manualSays: >-
      /db/EPSE's Specifications table gives one shape for both products.
    actual: >-
      The two products declare four fields between them that the other does
      not: `LOAD_TYPE` and `WIDTH` on Civil NX, `LOADING_AREA_GROUP` and
      `SEL_TYPE` on Gen NX. The Civil pair was in no contract at all; the Gen
      pair was in this one untagged, claimed for both.
    evidence: >-
      GET /info/db/EPSE on both products, captured 2026-09-03, compared by
      scripts/info_baseline.py --divergence. Registered as MD-46.

sdkRules:
  - id: db-epse-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-epse-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 06_DB_Static_Loads.md line 1588
  table: 파라미터
