# /db/BCCT
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: db-bcct
endpoint: /db/BCCT
name: Boundary Change Assignment
surface:
  className: BoundaryChangeAssignment
  exportName: boundaryChangeAssignment
  modulePath: [db, analysisControl]
  payloadTypeName: BoundaryChangeAssignmentPayload
  nestedTypes:
    - {path: vBOUNDARY, name: BoundaryGroupCombinationItem, namespace: DbAnalysisControlTypes}
    - {path: vLOADANAL, name: LoadCaseAnalysisAssignmentItem, namespace: DbAnalysisControlTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 12_DB_Analysis_Control.md
    section: 19. /db/BCCT — Boundary Change Assignment
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-write-sweep-2026-08-16
    - product: gen
      ref: db-write-sweep-2026-08-16

operations:
  - method: DELETE
    variant: per_id
    path: /db/BCCT/{id}
    risk: destructive
    mitigation: none
    summary: Delete exactly the record named in the path.
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
    notes: >-
      Undocumented route, and the only DELETE form that deletes a selection
      rather than the whole table - see risk db-delete-body-empties-table.
  - method: DELETE
    variant: whole_table
    risk: destructive
    mitigation: confirmation_required
    summary: Empty the entire table.
    request:
      wrapper: assign
      itemSchema: none
      description: >-
        The manual's documented form. The ids in that body are ignored.
    response:
      wrapper: message
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: bSPT
    description: >-
      Support
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bSPR
    description: >-
      Point Spring Support
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bGSPR
    description: >-
      General Spring Support
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bCGLINK
    description: >-
      Change General Link Property
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bSSSF
    description: >-
      Section Stiffness Scale Factor
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bPSSF
    description: >-
      Plate Stiffness Scale Factor
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bRLS
    description: >-
      Beam End Release
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: bWSSF
    description: >-
      Wall Stiffness Scale Factor
    products: [gen]
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's BoundaryChangeAssignment case completed a
      live create-read-update-delete round trip on ('gen',) without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: bESSF
    description: >-
      Element Stiffness Scale Factor
    products: [gen]
    # NOTE: /info evidence, not the manual's. GET /info/db/BCCT declares this
    # on gen and not on the other product, captured 2026-09-03. The section
    # says nothing about either product, so this tag records a measurement
    # rather than a documentation claim. See MD-46.
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: true
    omissionEvidence: >-
      scripts/live_crud_check.py's BoundaryChangeAssignment case completed a
      live create-read-update-delete round trip on ('gen',) without this field
      in its payload. That is evidence the call is accepted, not that the
      resulting model is what an engineer wanted.
    provenance: manual
  - key: bCDOF
    description: >-
      Constrain DOF associated with specified displacements / settlements by
      boundary group combinations
    type: boolean
    requirement: optional
    documentedDefault: false
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
  - key: vBOUNDARY
    description: >-
      Boundary List
    type: array
    items:
      type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: BGCNAME
        description: >-
          Boundary Group Combination Name
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vBOUNDARY' by numbering it '(1)', not
        # by naming a path
      - key: vBG
        description: >-
          Boundary Group List
        type: array
        items:
          type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vBOUNDARY' by numbering it '(2)', not
        # by naming a path
  - key: vLOADANAL
    description: >-
      Load Cases & Analysis List
    type: array
    items:
      type: object
    requirement: optional
    documentedDefault: null
    documentedOptional: true
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: TYPE
        description: >-
          Load Cases & Analysis Type
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vLOADANAL' by numbering it '(1)', not
        # by naming a path
      - key: BGCNAME
        description: >-
          Boundary Group Combination Name
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vLOADANAL' by numbering it '(2)', not
        # by naming a path
      - key: LCNAME
        description: >-
          Static Load Case
        type: string
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        # RESOLVED: the manual nests this under 'vLOADANAL' by numbering it '(3)', not
        # by naming a path

sdkRules:
  - id: db-bcct-delete-per-id
    kind: per_id_request
    appliesTo: [DELETE]
    variant: per_id
    reason: >-
      The manual's DELETE body form empties the whole table regardless of the
      ids it names. Measured on /db/NODE, /db/STLD, /db/LDGR and /db/MATL and
      treated as a property of /db/* DELETE, so an SDK's ordinary "delete these
      ids" call must use the undocumented per-id URL. Requests go one at a time
      and stop at the first failure.
    riskRef: db-delete-body-empties-table
    alternative: Emptying the table on purpose is the separate whole_table operation.
  - id: db-bcct-delete-all-confirmation
    kind: require_confirmation
    appliesTo: [DELETE]
    variant: whole_table
    reason: >-
      Emptying the table cannot be undone through the API and raises no
      product-side dialog.
    riskRef: db-delete-body-empties-table
    alternative: Use the per_id DELETE operation to remove selected records.

knownDefects:
  - ref: db-delete-body-empties-table

extraction:
  source: 12_DB_Analysis_Control.md line 2599
  table: Parameters — 데이터 선택 플래그
  structuralTables:
    - heading: "Parameters — Boundary List (`vBOUNDARY`)"
      line: 2614
      paths:
        - "<root>"
    - heading: "Parameters — Load Cases & Analysis List (`vLOADANAL`)"
      line: 2622
      paths:
        - "<root>"
