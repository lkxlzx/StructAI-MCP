# /DESIGN/RC/KDS-41-20-2022/DCRM-WALL
#
# Drafted by scripts/extract_contracts.py from the official manual, then promoted
# by scripts/promote_contract.py: products and verification status come from
# docs/coverage.json's live record, not from the manual's framing. For /db/*
# endpoints, the DELETE shape comes from measured /db/* DELETE behaviour.
# safeToOmit is answered `true` only where a confirmed live payload actually
# omitted the field.
contractVersion: 1
id: design-rc-kds-41-20-2022-dcrm-wall
endpoint: /DESIGN/RC/KDS-41-20-2022/DCRM-WALL
name: Design Criteria for Rebars by Wall Member
surface:
  className: RebarDesignCriteriaByWallMember
  exportName: rebarDesignCriteriaByWallMember
  modulePath: [design, rcKds, rebar]
  payloadTypeName: RebarDesignCriteriaByWallMemberPayload
  nestedTypes:
    - {path: Assign.ITEMS, name: RebarDesignCriteriaByWallMemberItem, namespace: DesignRcKdsRebarTypes}

products: [civil, gen]

source:
  manual:
    status: documented
    repo: Dennis5882/MIDAS-API
    chapterFile: 26_Design_RC_KDS41202022.md
    section: "32. `DESIGN/RC/KDS-41-20-2022/DCRM-WALL` — Design Criteria for Rebars by Wall Member (벽체 부재별 철근 설계기준)"
  liveNotes:
    - docs/live_verification_notes.md

verification:
  status: verified_both
  records:
    - product: civil
      ref: db-read-sweep-2026-07-26
    - product: gen
      ref: db-read-sweep-2026-07-26

operations:
  - method: DELETE
    risk: destructive
    mitigation: none
    request:
      wrapper: none
    response:
      wrapper: unknown
    notes: >-
      The manual shows a bodyless DELETE at this endpoint, but does not
      state the deletion scope or response shape.
  - method: GET
    risk: read_only
    request:
      wrapper: none
    response:
      wrapper: table
      keyStability: stable
  - method: POST
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable
  - method: PUT
    risk: write
    request:
      wrapper: assign
      itemSchema: fields
    response:
      wrapper: table
      keyStability: stable

fields:
  - key: Assign
    description: >-
      벽체 ID 문자열을 키로 갖는 맵
    type: object
    requirement: required
    documentedDefault: null
    documentedOptional: false
    safeToOmit: unverified
    provenance: manual
    properties:
      - key: ITEMS
        description: >-
          해당 벽체 ID의 층별 지정 목록 (min 1)
        type: array
        items:
          type: object
        minItems: 1
        requirement: required
        documentedDefault: null
        documentedOptional: false
        safeToOmit: unverified
        provenance: manual
        properties:
          - key: STORY
            description: >-
              층 이름 (최소 1자)
            type: string
            minLength: 1
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: VERTICAL_REBAR
            description: >-
              수직 철근 규격 · 19종 (D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual's own description says this has 19 values and lists
            # 5; the list is a sample, so no enum is transcribed
          - key: HORIZONTAL_REBAR
            description: >-
              수평 철근 규격 · 19종 (D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual's own description says this has 19 values and lists
            # 5; the list is a sample, so no enum is transcribed
          - key: END_REBAR
            description: >-
              단부 철근 규격 · 19종 (D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual's own description says this has 19 values and lists
            # 5; the list is a sample, so no enum is transcribed
          - key: BE_HORZ_REBAR
            description: >-
              경계요소 수평 철근 규격 · 19종 (D4 ~ D57)
            type: string
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
            # RESOLVED: the manual's own description says this has 19 values and lists
            # 5; the list is a sample, so no enum is transcribed
          - key: BE_HORZ_SPACE
            description: >-
              경계요소 수평 철근 간격
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: BE_VERT_SPACE
            description: >-
              경계요소 수직 철근 간격
            type: number
            requirement: required
            documentedDefault: null
            documentedOptional: false
            safeToOmit: unverified
            provenance: manual
          - key: DE
            description: >-
              단부 피복 거리 de (m)
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual
          - key: DW
            description: >-
              벽면 피복 거리 dw (m)
            type: number
            requirement: optional
            documentedDefault: 0
            documentedOptional: true
            safeToOmit: unverified
            provenance: manual

extraction:
  source: 26_Design_RC_KDS41202022.md line 5527
  table: Root / Item
  structuralTables:
    - heading: ITEMS 배열 항목 (층별 배근 지정)
      line: 5534
      paths:
        - Assign.ITEMS
