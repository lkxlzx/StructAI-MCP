[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "midas-mcp"
version = "1.0.0"
description = "Zero-dependency MIDAS NX Open API MCP connector"
requires-python = ">=3.9"

[tool.setuptools]
package-dir = {"" = "src"}
packages = ["midas_mcp"]
