# MIDAS API interface extraction — coverage report

Generated by [`extract_interfaces.py`](extract_interfaces.py).  Every number below is computed from the run, not estimated.  The pipeline takes the manuals' **index tables** as the primary source and consults the interface bodies only for the request schema.

## 1. Characterisation: the three manuals' shapes

### 1.1 Index tables

All three manuals carry the **same** index-table header, verbatim:

```text
| # | 接口代码 | 接口名称 | URL | 方法 | 所在章节 |
| --- | --- | --- | --- | --- | --- |
| 97 | `CNLD` | [Nodal](#nodal) | `db/CNLD` | GET / POST / PUT / DELETE | JSON数据手册 / Load |
```

One row therefore already carries `interface_code` + endpoint + method + chapter — everything `tool_interfaces` needs except the schema.  The table is the primary source for exactly that reason.

| manual | index header line | first row | last row | rows | cell anomalies |
| --- | --- | --- | --- | --- | --- |
| `gen` | 1308 | 1310 | 2190 | 881 | 0 |
| `civilnx` | 1047 | 1049 | 1502 | 454 | 0 |
| `designer` | 380 | 382 | 419 | 38 | 0 |

Per-manual detail:

**`gen`** — 410,728 lines

- index rows **881**, of which **307** carry a non-empty `接口代码` and **574** carry `—`; **13** state no URL at all, of which **0** were recovered from the entry's own title path or body and **13** document no path anywhere.
- index rows per `##` section: `接口总索引` 881
- distinct `方法` spellings: `POST / GET / PUT / DELETE` ×321; `POST` ×308; `GET / POST / PUT / DELETE` ×159; `GET / PUT / DELETE` ×27; `GET / POST / PUT` ×26; `GET / PUT` ×9; `POST / GET / DELETE / PUT` ×6; `POST / GET / PUT` ×5; `—` ×4; `GET` ×4; `GET / DELETE` ×3; `POST / PUT / DELETE / GET` ×3; `GET / POST` ×2; `PUT / GET / DELETE` ×2; `PUT / DELETE` ×1; `GET / DELETE / PUT` ×1
- distinct `所在章节` values: **54**; sub-chapter values: **47**

**`civilnx`** — 157,103 lines

- index rows **454**, of which **295** carry a non-empty `接口代码` and **159** carry `—`; **0** state no URL at all, of which **0** were recovered from the entry's own title path or body and **0** document no path anywhere.
- index rows per `##` section: `接口总索引` 454
- distinct `方法` spellings: `POST` ×225; `GET / POST / PUT / DELETE` ×206; `GET / POST / PUT` ×12; `GET / PUT` ×6; `GET` ×3; `—` ×1; `GET / DELETE` ×1
- distinct `所在章节` values: **79**; sub-chapter values: **73**

**`designer`** — 5,322 lines

- index rows **38**, of which **0** carry a non-empty `接口代码` and **38** carry `—`; **0** state no URL at all, of which **0** were recovered from the entry's own title path or body and **0** document no path anywhere.
- index rows per `##` section: `接口总索引` 38
- distinct `方法` spellings: `POST` ×24; `GET / PUT` ×8; `GET` ×3; `GET / POST / DELETE` ×3
- distinct `所在章节` values: **5**; sub-chapter values: **0**

### 1.2 Body shapes

Each manual states a body URI in a different way, and the difference is what broke the previous revision:

| manual | entry heading | body URI form | schema form |
| --- | --- | --- | --- |
| `gen` (JSON数据手册) | `#####`/`######` under `####` groups, preceded by `接口代码：`CODE`` | bold paragraph `** {base url} + db/CNLD**` — a space after `**` and a trailing U+200B on some entries, neither on others | `**JSON Schema**` (draft-07) or `**Input Data Form**` (`__DESC__`/`__TYPE__`) |
| `gen` (English sections) | `####` per interface | `##### **Input URI**` block, the URI in a one-cell table `| **/DESIGN/RC/KDS-41-20-2022/CMFT** |` or a fenced block | `##### **JSON Schema**` (draft-07), often with `enum`/`default` |
| `civilnx` | `####` per interface | `##### Input URI` block with `**{base url} + doc/new**`, or the bold paragraph form `**Input URI**` | `##### JSON Schema` (draft-07) for body-carrying interfaces; `**JSON Schema**` for some in the Results chapter |
| `designer` | `###` per interface (`####` are its blocks) | `#### 接口URL` then `**{base url} + /DOC/OPEN**` | **no** JSON Schema at all — only `#### 请求示例` and parameter tables |

Measured body/schema availability per manual:

| manual | body URI claims | distinct body URIs | JSON Schema | Input Data Form | example only | none |
| --- | --- | --- | --- | --- | --- | --- |
| `gen` | 1070 | 428 | 624 | 0 | 21 | 425 |
| `civilnx` | 978 | 232 | 71 | 0 | 219 | 688 |
| `designer` | 39 | 38 | 0 | 0 | 38 | 1 |

### 1.3 Do index rows and bodies join?

| manual | index rows | index URIs | body URIs | joined | index-only | body-only |
| --- | --- | --- | --- | --- | --- | --- |
| `gen` | 881 | 418 | 428 | 418 | 0 | 10 |
| `civilnx` | 454 | 228 | 232 | 228 | 0 | 4 |
| `designer` | 38 | 37 | 38 | 37 | 0 | 1 |

The join is on the **canonical URI** (lower-cased, `info/` stripped, `{base url} +` stripped), never on line numbers.

## 2. Entries in, rows out

| manual | adapter | index rows | body-only entries | entry records | rows out | merged |
| --- | --- | --- | --- | --- | --- | --- |
| `gen` | `midas_gen` | 881 | 10 | 891 | 1544 | 305 |
| `civilnx` | `midas_civil` | 454 | 4 | 458 | 922 | 80 |
| `designer` | `midas_cdn` | 38 | 1 | 39 | 51 | 1 |

**1373** index rows + **15** body-only entries = **1388** entry records, which expand to **2517** rows. Rows differ from entries for three reasons, in this order:

1. **Multi-method expansion** — one row per method.  A URI advertising `GET / POST / PUT / DELETE` yields four rows, because `method` is part of the row identity and the adapter dispatches per method.  The method is folded into `interface_code`, so this is not a collision.
2. **The same `(endpoint, method)` documented twice is merged** — the Gen manual repeats most of its Chinese-backbone interfaces in its English sections.  The later record is kept as `metadata_json.merged_records` on the surviving row, never dropped: **386** merges, listed in §6.
3. **Entries with no URI are not rows** — a row without an endpoint cannot be dispatched.  They are listed in §5 instead of being emitted with an empty endpoint.

The count to compare against a URI-level estimate is therefore **not** the row count.  Measured: `gen` 881 index rows → 418 distinct URIs, `civilnx` 454 index rows → 228 distinct URIs, `designer` 38 index rows → 37 distinct URIs.  One row exists per distinct `(endpoint, method)` pair, so a URI advertising four methods contributes four rows and a URI advertising one contributes one — which is why the row count lands above the distinct-URI count rather than below it.

Rows per adapter and per method:

| adapter | rows |
| --- | --- |
| `midas_cdn` | 51 |
| `midas_civil` | 922 |
| `midas_gen` | 1544 |

| method | rows |
| --- | --- |
| `DELETE` | 499 |
| `GET` | 551 |
| `POST` | 933 |
| `PUT` | 534 |

## 3. Schema coverage (the LLM-safety question)

- rows emitted: **2517**
- rows with a non-null `request_schema_json`: **1419** (**56.4%** of all rows)
- rows whose method carries a request body (`POST`/`PUT`/`PATCH`): **1467**, of which **1419** have a schema (**96.7%**) — **this is the denominator that matters**
- rows with `null`: 1098

`request_schema_json` is emitted **only for body-carrying methods**.  The manual documents one body per URI, so attaching it to the `GET`/`DELETE` rows of the same URI would invite a caller to send a body those verbs do not take.  A `null` therefore means one of two things, and they are not the same backlog item:

- the method takes no body (`GET`/`DELETE`) — nothing to validate, and
- the method takes a body but the manual documents none — **this** is the real gap, because such a call cannot be validated before it is sent.

Where the schema came from (`json_schema` = the manual's draft-07 JSON Schema, `input_data_form` = the Chinese backbone's `__DESC__`/`__TYPE__` form, `request_example` = the manual's own request-example block, `example` = a JSON fence found by position alone, because the entry states no marker this pipeline reads).  The counter is the authority on which of them actually occur: on this manual revision `input_data_form` does not, and §10 says why.

| `schema_source` | body-carrying rows |
| --- | --- |
| `example` | 346 |
| `json_schema` | 1037 |
| `request_example` | 36 |
| `None` | 48 |

When one URI is documented more than once the winner is chosen by **source quality first** — `json_schema` > `input_data_form` > `request_example` > `example` — then by the row's own title, then by section authority (`SECTION_PRIORITY`), then by file order.  Ordering by section first would let an English entry that carries only an example displace a Chinese entry that carries a form; the counter above is what records which source won.

### 3.1 `JSON Schema` markers seen vs schemas attached

Every manual states its request schema under a `JSON Schema` block marker, and the marker is written three ways: a bold paragraph (`**JSON Schema**`, Civil NX's Results chapter and nine Gen English entries), a plain level-5 heading (`##### JSON Schema`, Civil NX) and an emphasised level-5 heading (`##### **JSON Schema**`, both Gen English sections).  The collector matches on the marker's **name**, so all three count.  The two columns below are the check that matters: a section holding hundreds of markers and returning a handful of claims is the silent-zero failure mode, and it has to be visible as a number.

| manual | `##` section | `JSON Schema` markers | body claims | schemas attached | claims whose URI is not in the index | other sources |
| --- | --- | --- | --- | --- | --- | --- |
| `gen` | `JSON数据手册` | 0 | 424 | 0 | 0 | `example` 9, `none` 415 |
| `gen` | `英文数据手册（未编入索引）` | 171 | 171 | 170 | 12 | `request_example` 1 |
| `gen` | `英文数据手册（索引接口）` | 462 | 475 | 454 | 0 | `example` 11, `none` 10 |
| `civilnx` | `Pushover` | 0 | 12 | 0 | 0 | `example` 4, `none` 8 |
| `civilnx` | `URL` | 0 | 1 | 0 | 1 | `none` 1 |
| `civilnx` | `一、产品与 API 概览` | 0 | 1 | 0 | 1 | `none` 1 |
| `civilnx` | `分析Analysis` | 0 | 45 | 0 | 0 | `example` 16, `none` 29 |
| `civilnx` | `协同Collaboration` | 0 | 5 | 0 | 0 | `example` 5 |
| `civilnx` | `工具Apps` | 0 | 8 | 0 | 0 | `example` 3, `none` 5 |
| `civilnx` | `提示与技巧 ⭐` | 0 | 2 | 0 | 2 | `none` 2 |
| `civilnx` | `文件Documents` | 9 | 9 | 9 | 0 | — |
| `civilnx` | `特性Properties` | 25 | 60 | 25 | 0 | `example` 18, `none` 17 |
| `civilnx` | `结构Structure` | 1 | 1 | 1 | 0 | — |
| `civilnx` | `结果Results` | 10 | 476 | 10 | 0 | `example` 43, `none` 423 |
| `civilnx` | `节点/单元Node/Element` | 9 | 9 | 9 | 0 | — |
| `civilnx` | `荷载Load` | 1 | 268 | 1 | 0 | `example` 100, `none` 167 |
| `civilnx` | `视图View` | 9 | 12 | 9 | 0 | `example` 2, `none` 1 |
| `civilnx` | `设计Design` | 0 | 16 | 0 | 0 | `example` 5, `none` 11 |
| `civilnx` | `边界Boundary` | 0 | 45 | 0 | 0 | `example` 22, `none` 23 |
| `civilnx` | `项目Project` | 7 | 8 | 7 | 0 | `example` 1 |
| `designer` | `DB 数据库` | 0 | 13 | 0 | 0 | `request_example` 13 |
| `designer` | `DOC 文档` | 0 | 14 | 0 | 0 | `request_example` 14 |
| `designer` | `OPRT 操作` | 0 | 6 | 0 | 0 | `request_example` 6 |
| `designer` | `POST 后处理` | 0 | 1 | 0 | 0 | `request_example` 1 |
| `designer` | `VIEW 视图` | 0 | 4 | 0 | 0 | `request_example` 4 |
| `designer` | `入门学习` | 0 | 1 | 0 | 1 | `none` 1 |

Per-manual totals and the entry shapes measured, not assumed:

| manual | markers | attached | attached % | body claims | distinct body URIs | `Input URI` marker forms seen |
| --- | --- | --- | --- | --- | --- | --- |
| `gen` | 633 | 624 | 98.6% | 1070 | 428 | `heading emphasised` ×644, `bold emphasised` ×209, `heading plain` ×1 |
| `civilnx` | 71 | 71 | 100.0% | 978 | 232 | `bold emphasised` ×296, `heading plain` ×158 |
| `designer` | 0 | 0 | — | 39 | 38 | `heading plain` ×38 |

The Gen manual's English sections are where the previous revision failed: it recognised the bold `**Input URI**` paragraph, which is how the Chinese backbone states the marker, but not the `##### **Input URI**` **heading**, which is how both English sections state it.  Every one of their `JSON Schema` blocks was therefore unreachable — no entry, no attachment — and the counts above are what make that visible instead of silent.  The entry shapes actually found, by reading several sections of each kind:

| manual / section | entry heading | `Input URI` marker | URI value | method | schema |
| --- | --- | --- | --- | --- | --- |
| `gen` / JSON数据手册 | `####` under a `###` group, `接口代码：`CODE`` on its own line | `**Input URI**` bold paragraph, with one exception in the whole section that writes it as a `##### Input URI` heading | bold paragraph `** {base url} + db/CNLD**` — a space after `**` and a trailing U+200B on some entries, neither on others | bold paragraph `** Post**` | `**JSON Schema**` or `**Input Data Form**` + an indented fence |
| `gen` / 英文数据手册（索引接口） | `####`, with a `- 英文原文： [Title](url)` line under it | `##### **Input URI**` heading (the dominant form) or, for a couple of entries, the `**Input URI**` bold paragraph | one-cell table `| **{base url} + doc/NEW** |` | one-cell table `| **POST** |` | `##### **JSON Schema**` + a column-0 ```` ```json ```` fence; a few entries write the marker as the bold `**Json Schema**` paragraph |
| `gen` / 英文数据手册（未编入索引） | `####`, titled `PATH : Name` | `##### **Input URI**` heading, every entry | one-cell table `| **{base url} + DESIGN/SECT** |` | one-cell table `| **POST,GET,PUT,DELETE** |` | `##### **JSON Schema**`, every entry |
| `civilnx` | `####`, titled `中文[CODE]English` | `##### Input URI` heading (no emphasis) | bold paragraph `**{base url} + doc/new**` | `##### Active Methods` + bold `**Post**` | `##### JSON Schema` heading, or `##### Input Data Form` |
| `designer` | `###` per interface (`####` are its blocks) | `#### 接口URL` then `**{base url} + /DOC/OPEN**` | bold paragraph | bold paragraph | **no** JSON Schema — `#### 请求示例` and parameter tables only |

**17** body claims state a URI the index table does not carry, or state the `Input URI` marker and no path at all.  They are **counted**, not dropped: a claim with a URI becomes a body-only entry (reported in §2), and a claim without one is listed in §5.  The first few, with their reason:

| manual | `##` section | claim |
| --- | --- | --- |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/cmft` line 348758 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/eqct` line 356493 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/mbtp` line 360237 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/mcmb` line 360405 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/mllr` line 360682 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/mrft` line 360887 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/sdgn` line 370390 — URI not in the index table |
| `gen` | `英文数据手册（未编入索引）` | `/design/rc/kds-41-20-2022/sueq` line 370670 — URI not in the index table |
| `civilnx` | `URL` | `/resource` line 3307 — URI not in the index table |
| `civilnx` | `一、产品与 API 概览` | `/url` line 110 — URI not in the index table |
| `civilnx` | `提示与技巧 ⭐` | `/midas` line 2777 — URI not in the index table |
| `civilnx` | `提示与技巧 ⭐` | `/db...` line 2810 — URI not in the index table |
| `designer` | `入门学习` | `/midas` line 4934 — URI not in the index table |

### 3.2 The shared result-table URIs

`/post/TABLE` and `/DESIGN/**/TABLE` are one URI each, carrying hundreds of logical tables selected by `TABLE_TYPE` inside the `Argument`.  A schema found on such a URI therefore describes exactly **one** table, and attaching it to every row on the URI would be worse than attaching nothing.  Two things keep it straight:

1. **The row's own title picks the claim.**  `best_body` prefers the claim whose entry title equals the index row's title when exactly one candidate matches, so `#### Beam Force - Analysis Result Table` supplies the schema for the row titled `Beam Force - Analysis Result Table` and not for any of the other rows sharing `/post/TABLE`.  Only a unique match is used; an ambiguous title falls back to the URI-level ranking rather than guessing.
2. **The literal `TABLE_TYPE` is read out of the entry and recorded.**  The value the API actually matches on is stated in the entry's own text, and it is recorded in `metadata_json.table_type`.  It is deliberately **not** used to re-key `interface_code`: the code is the row's published identity, and renaming it because a second spelling of the same discriminator turned up would move every consumer's key for no gain.

**Does the English side discriminate better than the Chinese side?  Yes, and measurably so.**  An English result-table entry states the literal three times — in its `JSON Schema` block (`"TABLE_NAME": "BEAMDESIGNFORCES"`), in its `Request Examples` body (`"TABLE_TYPE": "BEAMDESIGNFORCES"`) and in its `Specifications` row (`Result Table Type • "BEAMDESIGNFORCES" | "TABLE_TYPE" | …`) — where the Chinese entries state it once, in a per-entry example body, plus one generic `TABLE_TYPE` lookup table shared by all of them.  Measured per section (an entry that names several tables at once is counted as `ambiguous` and yields no single literal, because a discriminator that does not discriminate is worse than none):

| manual | `##` section | shared-URI entries | one literal | ambiguous | none |
| --- | --- | --- | --- | --- | --- |
| `gen` | `JSON数据手册` | 58 | 0 | 27 | 31 |
| `gen` | `英文数据手册（未编入索引）` | 18 | 6 | 12 | 0 |
| `gen` | `英文数据手册（索引接口）` | 120 | 40 | 79 | 1 |
| `civilnx` | `工具Apps` | 2 | 0 | 0 | 2 |
| `civilnx` | `结果Results` | 283 | 63 | 79 | 141 |
| `civilnx` | `节点/单元Node/Element` | 1 | 0 | 1 | 0 |
| `civilnx` | `荷载Load` | 3 | 0 | 3 | 0 |
| `designer` | `POST 后处理` | 1 | 0 | 0 | 1 |

Of the **314** emitted rows on a shared result-table URI, **75** carry a literal `metadata_json.table_type`.  The `TABLE_TYPE` slug in `interface_code` is still derived from the title for every one of them, so the codes are unchanged; the literal is the manual's own statement of the same fact and is what a caller should send.

## 4. The operation mapping

| method | operation | condition |
| --- | --- | --- |
| `GET` | `read` | always |
| `PUT` | `update` | always |
| `DELETE` | `delete` | always |
| `PATCH` | `update` | always |
| `POST` | `query` | title contains one of `table`, `result`, `graph`, `diagram`, `summary`, `response`, `return`, `取值`, `返回值`, `表格`, `结果` (checked first) |
| `POST` | `execute` | title contains one of `perform`, `check`, `anal`, `run`, `calculate`, `calc`, `generate`, `execute`, `evaluate`, `divide`, `auto-mesh`, `automesh`, `optimiz`, `rating`, `design`, `report`, `capture`, `screenshot`, `export`, `import`, `update` |
| `POST` | `create` | otherwise |

Query is checked before execute because the manual has titles such as `RC Beam Design Perform` and `Beam Force - Analysis Result Table`; reading the second as an execution would mis-file hundreds of result tables.

| operation | rows |
| --- | --- |
| `create` | 586 |
| `delete` | 499 |
| `execute` | 167 |
| `query` | 180 |
| `read` | 551 |
| `update` | 534 |

## 5. Entries with no URI

`inventory.json` records `uri: null` for a set of Gen entries.  The URIs are **not** absent from the manual: the Gen index table itself leaves the `URL` cell as `—` for those rows, and the manual then states the URI either as the entry title's own path prefix (`DESIGN/RC/KDS-41-20-2022/CMFT : …`) or in a one-cell table under an `Input URI` marker (`| **/DESIGN/RC/KDS-41-20-2022/CMFT** |`).  The previous revision looked only for `**{base url} + …**`, which is why it saw none of them.  This pipeline reads all three URI forms and recovers the URI, so those entries *are* emitted as rows; this section keeps the inventory's `null` auditable.

### 5.1 `gen` — 13 entries with `uri: null` in `inventory.json`

| # | title | inventory line | inventory methods | URI recovered |
| --- | --- | --- | --- | --- |
| 1 | `DESIGN/RC/KDS-41-20-2022/CMFT : Equivalent Moment Correction Factor(Cm)` | 348759 | POST, GET, PUT, DELETE | `—` |
| 2 | `DESIGN/RC/KDS-41-20-2022/EQCT : Seismic Load Combination Type` | 356514 | POST, GET, PUT, DELETE | `—` |
| 3 | `DESIGN/RC/KDS-41-20-2022/MBTP : Modify Member Type` | 360282 | POST, GET, PUT, DELETE | `—` |
| 4 | `DESIGN/RC/KDS-41-20-2022/MCMB : Moment Calculation Method for Beam` | 360452 | POST, GET, PUT, DELETE | `—` |
| 5 | `DESIGN/RC/KDS-41-20-2022/MLLR : Modify Live Load Reduction Factor` | 360733 | POST, GET, PUT, DELETE | `—` |
| 6 | `DESIGN/RC/KDS-41-20-2022/MRFT : Moment Redistribution Factor` | 360940 | POST, GET, PUT, DELETE | `—` |
| 7 | `DESIGN/RC/KDS-41-20-2022/SDGN : Seismic Design Type` | 370461 | POST, GET, PUT, DELETE | `—` |
| 8 | `DESIGN/RC/KDS-41-20-2022/SUEQ : Scale up Factor for Earthquake` | 370745 | POST, GET, PUT, DELETE | `—` |
| 9 | `DESIGN/RC/KDS-41-20-2022/TRFT : Torsion Reduction Factor` | 374096 | POST, GET, PUT, DELETE | `—` |
| 10 | `DESIGN/STEEL/KDS-41-30-2022/SLRS : Seismic Load Resisting System by Member` | 391630 | POST, GET, PUT, DELETE | `—` |
| 11 | `Modify Material - Rebar` | 402200 | POST | `—` |
| 12 | `Modify Material - concrete` | 405963 | POST | `—` |
| 13 | `Steel Design Result` | 410124 | POST | `—` |

### 5.2 `civilnx` — 0 entries with `uri: null` in `inventory.json`

_none._

### 5.3 `designer` — 0 entries with `uri: null` in `inventory.json`

_none._

URIs recovered for index rows whose `URL` cell was `—`:

| manual | index line | title | chapter | URI | recovered from |
| --- | --- | --- | --- | --- | --- |

| manual | index rows stating no URL | recovered | still no URI | recovery routes |
| --- | --- | --- | --- | --- |
| `gen` | 13 | 0 | 13 | — |
| `civilnx` | 0 | 0 | 0 | — |
| `designer` | 0 | 0 | 0 | — |

The three rows that still document no URI are Gen entries whose own body says `| **{base url} +** |` — the manual names the marker and then states no path.  Emitting the bare base URL as an endpoint would be an invention, so they are counted here and not emitted.

## 6. `interface_code` construction and disambiguation

`interface_code` is minted as `{adapter}.{family}.{path…}.{operation}`, with the table discriminator inserted before the operation when the URI is one of the shared result-table URIs.  Examples:

```text
midas_gen.db.cnld.read       GET    /db/CNLD
midas_gen.db.cnld.create     POST   /db/CNLD
midas_gen.db.cnld.update     PUT    /db/CNLD
midas_gen.db.cnld.delete     DELETE /db/CNLD
midas_gen.db.lcom_conc.read  GET    /db/LCOM-CONC
midas_gen.post.table.beam_force.query   POST /post/TABLE   (TABLE_TYPE=BEAMFORCE)
midas_cdn.doc.open.create    POST   /doc/open
```

Two discriminators exist, and neither is a numeric suffix:

1. **Operation** — the method is part of the code, so a four-method URI produces four distinct codes by construction.  The previous revision emitted `midas_gen.db.cnld`, `.2`, `.3`, `.4`, which made the code carry no information.
2. **Table type** — `/post/TABLE` and `/DESIGN/**/TABLE` are one URI each carrying hundreds of result tables selected by `TABLE_TYPE` inside the `Argument` (总纲 裁决 B-3).  The title names the table, so the title becomes the discriminator.

**314** rows carry a table-type discriminator.  Rows per family:

| family | rows |
| --- | --- |
| `db` | 1708 |
| `design` | 390 |
| `post` | 301 |
| `ope` | 41 |
| `doc` | 36 |
| `view` | 17 |
| `rating` | 13 |
| `oprt` | 6 |
| `midas` | 2 |
| `db...` | 1 |
| `resource` | 1 |
| `url` | 1 |

`endpoint` alone is **not** unique, on purpose: the shared table URIs carry one row per `TABLE_TYPE` on the same URI and method.  **5** `(adapter, endpoint, method)` triples carry more than one row; the top 20:

| adapter | endpoint | method | rows |
| --- | --- | --- | --- |
| `midas_gen` | `/post/TABLE` | `POST` | 149 |
| `midas_civil` | `/post/TABLE` | `POST` | 147 |
| `midas_gen` | `/design/PSC/aashto-lrfd24/table` | `POST` | 8 |
| `midas_gen` | `/design/RC/kds-41-20-2022/table` | `POST` | 3 |
| `midas_gen` | `/design/SRC/aik-src2k/table` | `POST` | 2 |

### 6.1 Records that did not become their own row

- `same URI documented again — folded into the primary row`: **373**
- `no URI — no endpoint to dispatch`: **13**

**386** records total.  The first 60:

| adapter | endpoint | method | reason | kept line | merged line | merged section |
| --- | --- | --- | --- | --- | --- | --- |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/CMFT : Equivalent Moment Correction Factor(Cm)](#designrckds-41-20-2022cmft--equivalent-moment-correction-factorcm)` | `—` | `no URI — no endpoint to dispatch` | None | 2069 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/EQCT : Seismic Load Combination Type](#designrckds-41-20-2022eqct--seismic-load-combination-type)` | `—` | `no URI — no endpoint to dispatch` | None | 2079 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/MBTP : Modify Member Type](#designrckds-41-20-2022mbtp--modify-member-type)` | `—` | `no URI — no endpoint to dispatch` | None | 2091 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/MCMB : Moment Calculation Method for Beam](#designrckds-41-20-2022mcmb--moment-calculation-method-for-beam)` | `—` | `no URI — no endpoint to dispatch` | None | 2092 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/MLLR : Modify Live Load Reduction Factor](#designrckds-41-20-2022mllr--modify-live-load-reduction-factor)` | `—` | `no URI — no endpoint to dispatch` | None | 2094 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/MRFT : Moment Redistribution Factor](#designrckds-41-20-2022mrft--moment-redistribution-factor)` | `—` | `no URI — no endpoint to dispatch` | None | 2095 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/SDGN : Seismic Design Type](#designrckds-41-20-2022sdgn--seismic-design-type)` | `—` | `no URI — no endpoint to dispatch` | None | 2104 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/SUEQ : Scale up Factor for Earthquake](#designrckds-41-20-2022sueq--scale-up-factor-for-earthquake)` | `—` | `no URI — no endpoint to dispatch` | None | 2106 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/RC/KDS-41-20-2022/TRFT : Torsion Reduction Factor](#designrckds-41-20-2022trft--torsion-reduction-factor)` | `—` | `no URI — no endpoint to dispatch` | None | 2110 | `英文数据手册（未编入索引） / DESIGN/RC/KDS-41-20-2022` |
| `midas_gen` | `[DESIGN/STEEL/KDS-41-30-2022/SLRS : Seismic Load Resisting System by Member](#designsteelkds-41-30-2022slrs--seismic-load-resisting-system-by-member)` | `—` | `no URI — no endpoint to dispatch` | None | 2168 | `英文数据手册（未编入索引） / DESIGN/STEEL/KDS-41-30-2022` |
| `midas_gen` | `[Modify Material - Rebar](#modify-material---rebar)` | `—` | `no URI — no endpoint to dispatch` | None | 2185 | `英文数据手册（未编入索引） / 其他` |
| `midas_gen` | `[Modify Material - concrete](#modify-material---concrete)` | `—` | `no URI — no endpoint to dispatch` | None | 2186 | `英文数据手册（未编入索引） / 其他` |
| `midas_gen` | `[Steel Design Result](#steel-design-result)` | `—` | `no URI — no endpoint to dispatch` | None | 2189 | `英文数据手册（未编入索引） / 其他` |
| `midas_gen` | `/doc/NEW` | `—` | `same URI documented again — folded into the primary row` | 1310 | 1545 | `接口总索引` |
| `midas_gen` | `/doc/OPEN` | `—` | `same URI documented again — folded into the primary row` | 1311 | 1546 | `接口总索引` |
| `midas_gen` | `/doc/CLOSE` | `—` | `same URI documented again — folded into the primary row` | 1312 | 1547 | `接口总索引` |
| `midas_gen` | `/db/PJCF` | `—` | `same URI documented again — folded into the primary row` | 1313 | 1556 | `接口总索引` |
| `midas_gen` | `/doc/SAVE` | `—` | `same URI documented again — folded into the primary row` | 1314 | 1548 | `接口总索引` |
| `midas_gen` | `/doc/SAVEAS` | `—` | `same URI documented again — folded into the primary row` | 1315 | 1549 | `接口总索引` |
| `midas_gen` | `/doc/IMPORT` | `—` | `same URI documented again — folded into the primary row` | 1316 | 1551 | `接口总索引` |
| `midas_gen` | `/doc/IMPORTMXT` | `—` | `same URI documented again — folded into the primary row` | 1317 | 1552 | `接口总索引` |
| `midas_gen` | `/doc/EXPORT` | `—` | `same URI documented again — folded into the primary row` | 1318 | 1553 | `接口总索引` |
| `midas_gen` | `/doc/EXPORTMXT` | `—` | `same URI documented again — folded into the primary row` | 1319 | 1554 | `接口总索引` |
| `midas_gen` | `/view/CAPTURE` | `—` | `same URI documented again — folded into the primary row` | 1320 | 1321 | `接口总索引` |
| `midas_gen` | `/view/CAPTURE` | `—` | `same URI documented again — folded into the primary row` | 1320 | 1859 | `接口总索引` |
| `midas_gen` | `/doc/ANAL` | `—` | `same URI documented again — folded into the primary row` | 1322 | 1464 | `接口总索引` |
| `midas_gen` | `/doc/ANAL` | `—` | `same URI documented again — folded into the primary row` | 1322 | 1555 | `接口总索引` |
| `midas_gen` | `/db/NPLN` | `—` | `same URI documented again — folded into the primary row` | 1323 | 1564 | `接口总索引` |
| `midas_gen` | `/db/CO_T` | `—` | `same URI documented again — folded into the primary row` | 1325 | 1326 | `接口总索引` |
| `midas_gen` | `/db/CO_T` | `—` | `same URI documented again — folded into the primary row` | 1325 | 1567 | `接口总索引` |
| `midas_gen` | `/db/CO_M` | `—` | `same URI documented again — folded into the primary row` | 1327 | 1328 | `接口总索引` |
| `midas_gen` | `/db/CO_M` | `—` | `same URI documented again — folded into the primary row` | 1327 | 1565 | `接口总索引` |
| `midas_gen` | `/db/CO_F` | `—` | `same URI documented again — folded into the primary row` | 1329 | 1330 | `接口总索引` |
| `midas_gen` | `/db/CO_F` | `—` | `same URI documented again — folded into the primary row` | 1329 | 1568 | `接口总索引` |
| `midas_gen` | `/db/CO_S` | `—` | `same URI documented again — folded into the primary row` | 1331 | 1332 | `接口总索引` |
| `midas_gen` | `/db/CO_S` | `—` | `same URI documented again — folded into the primary row` | 1331 | 1566 | `接口总索引` |
| `midas_gen` | `/db/UNIT` | `—` | `same URI documented again — folded into the primary row` | 1333 | 1557 | `接口总索引` |
| `midas_gen` | `/db/STYP` | `—` | `same URI documented again — folded into the primary row` | 1334 | 1558 | `接口总索引` |
| `midas_gen` | `/db/GRUP` | `—` | `same URI documented again — folded into the primary row` | 1335 | 1560 | `接口总索引` |
| `midas_gen` | `/db/BNGR` | `—` | `same URI documented again — folded into the primary row` | 1336 | 1561 | `接口总索引` |
| `midas_gen` | `/db/LDGR` | `—` | `same URI documented again — folded into the primary row` | 1337 | 1562 | `接口总索引` |
| `midas_gen` | `/db/TDGR` | `—` | `same URI documented again — folded into the primary row` | 1338 | 1563 | `接口总索引` |
| `midas_gen` | `/db/NODE` | `—` | `same URI documented again — folded into the primary row` | 1339 | 1571 | `接口总索引` |
| `midas_gen` | `/db/ELEM` | `—` | `same URI documented again — folded into the primary row` | 1340 | 1572 | `接口总索引` |
| `midas_gen` | `/db/SKEW` | `—` | `same URI documented again — folded into the primary row` | 1341 | 1573 | `接口总索引` |
| `midas_gen` | `/db/DOEL` | `—` | `same URI documented again — folded into the primary row` | 1342 | 1343 | `接口总索引` |
| `midas_gen` | `/db/DOEL` | `—` | `same URI documented again — folded into the primary row` | 1342 | 1576 | `接口总索引` |
| `midas_gen` | `/db/MADO` | `—` | `same URI documented again — folded into the primary row` | 1344 | 1574 | `接口总索引` |
| `midas_gen` | `/db/SBDO` | `—` | `same URI documented again — folded into the primary row` | 1345 | 1575 | `接口总索引` |
| `midas_gen` | `/db/MATL` | `—` | `same URI documented again — folded into the primary row` | 1346 | 1577 | `接口总索引` |
| `midas_gen` | `/db/EDMP` | `—` | `same URI documented again — folded into the primary row` | 1347 | 1584 | `接口总索引` |
| `midas_gen` | `/db/EPMT` | `—` | `same URI documented again — folded into the primary row` | 1348 | 1586 | `接口总索引` |
| `midas_gen` | `/db/EWSF` | `—` | `same URI documented again — folded into the primary row` | 1349 | 1616 | `接口总索引` |
| `midas_gen` | `/db/FIBR` | `—` | `same URI documented again — folded into the primary row` | 1350 | 1624 | `接口总索引` |
| `midas_gen` | `/db/FIMP` | `—` | `same URI documented again — folded into the primary row` | 1351 | 1623 | `接口总索引` |
| `midas_gen` | `/db/GRDP` | `—` | `same URI documented again — folded into the primary row` | 1352 | 1625 | `接口总索引` |
| `midas_gen` | `/db/IEHC` | `—` | `same URI documented again — folded into the primary row` | 1353 | 1617 | `接口总索引` |
| `midas_gen` | `/db/IEHG` | `—` | `same URI documented again — folded into the primary row` | 1354 | 1618 | `接口总索引` |
| `midas_gen` | `/db/IMFM` | `—` | `same URI documented again — folded into the primary row` | 1355 | 1579 | `接口总索引` |
| `midas_gen` | `/db/SECT` | `—` | `same URI documented again — folded into the primary row` | 1356 | 1357 | `接口总索引` |

### 6.2 Interface codes qualified to stay unique

_None._  The method plus the table discriminator made every `(adapter_code, interface_code)` unique on this run.

## 7. Rows per `domain` and per `feature`

| domain | rows |
| --- | --- |
| `load` | 723 |
| `design` | 491 |
| `model` | 463 |
| `result` | 315 |
| `analysis` | 274 |
| `project` | 144 |
| `None` | 42 |
| `view` | 36 |
| `operation` | 29 |

| feature | domain | rows |
| --- | --- | --- |
| `post_design` | `design` | 395 |
| `db_static_loads` | `load` | 358 |
| `post_analysis_result_1` | `result` | 276 |
| `db_analysis_control` | `analysis` | 231 |
| `db_properties` | `model` | 220 |
| `db_boundary` | `model` | 178 |
| `db_moving_loads` | `load` | 112 |
| `db_project_structure` | `project` | 102 |
| `db_construction_stage` | `load` | 88 |
| `db_design` | `design` | 83 |
| `db_temperature_prestress` | `load` | 76 |
| `db_node_element` | `model` | 49 |
| `db_dynamic_loads` | `load` | 46 |
| `db_settlement_misc_loads` | `load` | 43 |
| `None` | `` | 42 |
| `doc` | `project` | 42 |
| `db_pushover` | `analysis` | 39 |
| `view` | `view` | 36 |
| `ope` | `operation` | 29 |
| `post_story_tables` | `result` | 19 |
| `db_bridge` | `model` | 16 |
| `design_rc_kds41202022` | `design` | 13 |
| `post_analysis_result_2` | `result` | 10 |
| `post_pre_process` | `result` | 10 |
| `db_load_combinations` | `analysis` | 4 |

Feature attribution provenance (`chapter_table` = the manual's own `所在章节` column resolved through a table, `chapter_files` = the URI is listed by one of the 27 chapter files in `MIDAS-API-main/docs/manual/`, `fallback` = keyword rule, `unmapped` = no feature emitted).  The manual's own filing wins when it names a chapter, because that is the manual's own statement about where the endpoint belongs:

| provenance | entry records |
| --- | --- |
| `chapter_files` | 262 |
| `chapter_table` | 449 |
| `fallback` | 270 |
| `unmapped` | 21 |

**42** rows have no `feature` (the column is nullable):

| endpoint | title | chapter |
| --- | --- | --- |
| `/design/RC/kds-41-20-2022/cmft` | `DESIGN/RC/KDS-41-20-2022/CMFT : Equivalent Moment Correction Factor(Cm)` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/cmft` | `DESIGN/RC/KDS-41-20-2022/CMFT : Equivalent Moment Correction Factor(Cm)` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/eqct` | `DESIGN/RC/KDS-41-20-2022/EQCT : Seismic Load Combination Type` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/eqct` | `DESIGN/RC/KDS-41-20-2022/EQCT : Seismic Load Combination Type` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mbtp` | `DESIGN/RC/KDS-41-20-2022/MBTP : Modify Member Type` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mbtp` | `DESIGN/RC/KDS-41-20-2022/MBTP : Modify Member Type` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mcmb` | `DESIGN/RC/KDS-41-20-2022/MCMB : Moment Calculation Method for Beam` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mcmb` | `DESIGN/RC/KDS-41-20-2022/MCMB : Moment Calculation Method for Beam` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mllr` | `DESIGN/RC/KDS-41-20-2022/MLLR : Modify Live Load Reduction Factor` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mllr` | `DESIGN/RC/KDS-41-20-2022/MLLR : Modify Live Load Reduction Factor` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mrft` | `DESIGN/RC/KDS-41-20-2022/MRFT : Moment Redistribution Factor` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/mrft` | `DESIGN/RC/KDS-41-20-2022/MRFT : Moment Redistribution Factor` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/sdgn` | `DESIGN/RC/KDS-41-20-2022/SDGN : Seismic Design Type` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/sdgn` | `DESIGN/RC/KDS-41-20-2022/SDGN : Seismic Design Type` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/sueq` | `DESIGN/RC/KDS-41-20-2022/SUEQ : Scale up Factor for Earthquake` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/sueq` | `DESIGN/RC/KDS-41-20-2022/SUEQ : Scale up Factor for Earthquake` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/trft` | `DESIGN/RC/KDS-41-20-2022/TRFT : Torsion Reduction Factor` | `英文数据手册（未编入索引）` |
| `/design/RC/kds-41-20-2022/trft` | `DESIGN/RC/KDS-41-20-2022/TRFT : Torsion Reduction Factor` | `英文数据手册（未编入索引）` |
| `/design/STEEL/dstl` | `[Design Code](#design-code-1)` | `英文数据手册（未编入索引） / 其他` |
| `/design/STEEL/dstl` | `[Design Code](#design-code-1)` | `英文数据手册（未编入索引） / 其他` |
| `/design/STEEL/dstl` | `[Design Code](#design-code-1)` | `英文数据手册（未编入索引） / 其他` |
| `/design/STEEL/kds-41-30-2022/ocheck` | `[Steel Optimal Design](#steel-optimal-design)` | `英文数据手册（未编入索引） / 其他` |
| `/design/STEEL/kds-41-30-2022/slrs` | `DESIGN/STEEL/KDS-41-30-2022/SLRS : Seismic Load Resisting System by Member` | `英文数据手册（未编入索引）` |
| `/design/STEEL/kds-41-30-2022/slrs` | `DESIGN/STEEL/KDS-41-30-2022/SLRS : Seismic Load Resisting System by Member` | `英文数据手册（未编入索引）` |
| `/rating/psc/aashto-lrfr19/dfrc` | `[RATING/PSC/AASHTO-LRFR19/DFRC : Define Rating Case](#ratingpscaashto-lrfr19dfrc--define-rating-case)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/dfrc` | `[RATING/PSC/AASHTO-LRFR19/DFRC : Define Rating Case](#ratingpscaashto-lrfr19dfrc--define-rating-case)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/dfrc` | `[RATING/PSC/AASHTO-LRFR19/DFRC : Define Rating Case](#ratingpscaashto-lrfr19dfrc--define-rating-case)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/dfrc` | `[RATING/PSC/AASHTO-LRFR19/DFRC : Define Rating Case](#ratingpscaashto-lrfr19dfrc--define-rating-case)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro` | `[RATING/PSC/AASHTO-LRFR19/PFRO : Position for Rating Output](#ratingpscaashto-lrfr19pfro--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro` | `[RATING/PSC/AASHTO-LRFR19/PFRO : Position for Rating Output](#ratingpscaashto-lrfr19pfro--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro` | `[RATING/PSC/AASHTO-LRFR19/PFRO : Position for Rating Output](#ratingpscaashto-lrfr19pfro--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro` | `[RATING/PSC/AASHTO-LRFR19/PFRO : Position for Rating Output](#ratingpscaashto-lrfr19pfro--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro-vbeam` | `[RATING/PSC/AASHTO-LRFR19/PFRO-VBEAM : Position for Rating Output](#ratingpscaashto-lrfr19pfro-vbeam--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro-vbeam` | `[RATING/PSC/AASHTO-LRFR19/PFRO-VBEAM : Position for Rating Output](#ratingpscaashto-lrfr19pfro-vbeam--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro-vbeam` | `[RATING/PSC/AASHTO-LRFR19/PFRO-VBEAM : Position for Rating Output](#ratingpscaashto-lrfr19pfro-vbeam--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/pfro-vbeam` | `[RATING/PSC/AASHTO-LRFR19/PFRO-VBEAM : Position for Rating Output](#ratingpscaashto-lrfr19pfro-vbeam--position-for-rating-output)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/rating/psc/aashto-lrfr19/rpsc-anal` | `[RATING/PSC/AASHTO-LRFR19/RPSC-ANAL : Perform Rating](#ratingpscaashto-lrfr19rpsc-anal--perform-rating)` | `英文数据手册（未编入索引） / RATING/PSC/AASHTO-LRFR19` |
| `/db...` | `巧用URL中的“Info”` | `提示与技巧 ⭐` |
| `/midas` | `MIDAS API 里的 base URL & API Key` | `提示与技巧 ⭐` |
| `/resource` | `后续拓展（自行尝试）` | `URL` |

## 8. The unknown-family branch

These endpoints' first path segment is not one of `db`, `doc`, `ope`, `view`, `post`, `info`, `design`, so §3.1's wrapper rule does not cover them and both `request_wrapper` and `response_root_key` are left `null`:

| family | entry records |
| --- | --- |
| `db...` | 1 |
| `midas` | 2 |
| `oprt` | 6 |
| `resource` | 1 |
| `url` | 1 |

Wrapper and response-root-key distribution:

| `request_wrapper` | rows |
| --- | --- |
| `Argument` | 798 |
| `Assign` | 1708 |
| `None` | 11 |

`response_root_key` is `null` on **809** rows — every non-`/db/*` row, by §3.2.

## 9. Where the manuals contradict `inventory.json`

### 9.1 `gen`

- manual: **410,728** lines; inventory entries: **881**; index rows: **881**; entry records: **891**
- inventory entries with `uri: null`: **13**; index rows whose `URL` cell is `—`: **13** (the two sets are not the same: an entry can have a URL in the index table and `null` in the inventory, or the other way round)
- unique URIs — inventory **418**, index table **418**, bodies **428**, joined **418**
- URIs in `inventory.json` but in neither the index table nor a body: **0**
- URIs in the manual but not in `inventory.json`: **10**

Manual-only URIs (first 30):

```text
/design/rc/kds-41-20-2022/cmft
/design/rc/kds-41-20-2022/eqct
/design/rc/kds-41-20-2022/mbtp
/design/rc/kds-41-20-2022/mcmb
/design/rc/kds-41-20-2022/mllr
/design/rc/kds-41-20-2022/mrft
/design/rc/kds-41-20-2022/sdgn
/design/rc/kds-41-20-2022/sueq
/design/rc/kds-41-20-2022/trft
/design/steel/kds-41-30-2022/slrs
```

### 9.2 `civilnx`

- manual: **157,103** lines; inventory entries: **454**; index rows: **454**; entry records: **458**
- inventory entries with `uri: null`: **0**; index rows whose `URL` cell is `—`: **0** (the two sets are not the same: an entry can have a URL in the index table and `null` in the inventory, or the other way round)
- unique URIs — inventory **228**, index table **228**, bodies **232**, joined **228**
- URIs in `inventory.json` but in neither the index table nor a body: **0**
- URIs in the manual but not in `inventory.json`: **4**

Manual-only URIs (first 30):

```text
/db...
/midas
/resource
/url
```

### 9.3 `designer`

- manual: **5,322** lines; inventory entries: **38**; index rows: **38**; entry records: **39**
- inventory entries with `uri: null`: **0**; index rows whose `URL` cell is `—`: **0** (the two sets are not the same: an entry can have a URL in the index table and `null` in the inventory, or the other way round)
- unique URIs — inventory **37**, index table **37**, bodies **38**, joined **37**
- URIs in `inventory.json` but in neither the index table nor a body: **0**
- URIs in the manual but not in `inventory.json`: **1**

Manual-only URIs (first 30):

```text
/midas
```

### 9.4 Encoding damage

| manual | lines | lines with U+FFFD | lines with mojibake | lines with U+200B |
| --- | --- | --- | --- | --- |
| `gen` | 410,728 | 45 | 0 | 198 |
| `civilnx` | 157,103 | 42 | 0 | 9 |
| `designer` | 5,322 | 0 | 0 | 0 |

Read with `errors="replace"`; U+200B is stripped from every line before matching (the Gen manual writes `** {base url} + db/CNLD**\u200b` on some entries and `**{base url} + db/ACTL**` on others), and the occurrence count is reported rather than swallowed.  Mojibake is reported, never repaired.

`inventory.json`'s `line` numbers are **stale** — the Gen manual has **410,728** lines while the inventory's last Gen entry claims 410,979.  This pipeline therefore matches **by URI** and copies the *current* manual's line into `metadata_json.source_line`.

## 10. What could not be extracted, and why

1. **Civil Designer has no JSON Schema anywhere.**  Its 38 interfaces document `请求示例` and parameter tables (`参数名 | 数据类型 | 必填 | 说明`) only.  Their `request_schema_json` is therefore the request **example**, tagged `request_example`, or `null` when even that is absent — a parameter table is not a schema and is not turned into one here.
2. **The Chinese backbone's `Input Data Form` is not draft-07 — and its blocks are not always reachable.**  It gives `__DESC__` (description) and `__TYPE__` (type) per key and nothing else: no `required`, no `enum`, no `default`.  The marker is recognised, but two things in the manual keep it from winning.  Either the block is introduced by `- {base url} + info/db/<code>` followed by a **four-space-indented** fence, which this pipeline's fence rule (up to three spaces) does not treat as a fence at all; or it is introduced by `**{base url} + info/db/<code>**`, which is itself a claim for the same URI and therefore ends the enclosing claim's span one line early.  Measured: `input_data_form` is 0 both before and after this revision.  Where a fence *is* reachable the block is attached verbatim and tagged `example` (it is a JSON object, so it is a usable body), and those rows are superseded by the English `json_schema` wherever the English section documents the same URI — which §3 counts.
3. **`/post/*` and `/DESIGN/**/TABLE` share one URI per family.**  The `TABLE_TYPE` that selects the actual table lives inside the `Argument`.  The entry title is the discriminator the `interface_code` carries, and the **literal** the entry's own text states is recorded in `metadata_json.table_type` (§3.2).  A consumer must send the matching `TABLE_TYPE`; the code says which, and the metadata now says what the manual called it.
4. **`product_scope` is `unknown` for every row.**  The manuals' `Active Programs` / product labels are unreliable (对接规范 §3.5 第 15 条: 32 of 47 endpoints declared "Civil-only" answer on Gen NX too), so real values must come from live probing.  Emitting a label here would launder a manual claim into a capability.
5. **Entries with no URI** are recovered from the manual wherever the manual states the path (§5), including the 13 Gen entries that `inventory.json` records as `uri: null`.  Gen's index table states no URL for **13** rows; **0** of them were recovered from the entry's own title path or body, and **13** document no path anywhere — their body says `| **{base url} +** |`.  Those are counted and listed, not emitted with an invented endpoint.
6. **`domain` is never inferred independently.**  It comes from `CAPABILITY_FEATURE_DOMAIN` once `feature` is known, so the three-layer hierarchy cannot drift.

## 11. Self-validation

The pipeline re-opened `interfaces.json` and re-validated it: every required field present, `product_scope` in the closed set, every `feature` in the closed set and consistent with `domain`, `(adapter_code, interface_code)` unique, every `request_schema_json` parseable as a JSON object, and every row's `method` in the HTTP set.

It then checked the **collection** against the manuals' own marker counts, because a file can be perfectly well formed and still be missing most of what the manual states:

- the index table of every manual parsed to a non-zero row count — the `index_rows=0` / `anomalies=0` silent zero that started this;
- every `##` section holding `JSON Schema` markers has claims — a section with markers and no entry is the same silent zero in a smaller place;
- for every section holding at least **8** `JSON Schema` markers, at least **75%** of them were attached as `json_schema`; smaller sections must attach at least one;
- no manual reports `JSON Schema` markers with zero schemas attached.

Measured on this run: `gen` 633 marker(s) / 624 attached, `civilnx` 71 marker(s) / 71 attached, `designer` 0 marker(s) / 0 attached.  A shortfall exits non-zero **instead of** writing this report, so a wrong count can never be mistaken for a clean run.
